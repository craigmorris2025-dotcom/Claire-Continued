#!/usr/bin/env python3
from __future__ import annotations

import argparse
import json
import shutil
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Dict, List

INSTALLER_NAME = 'Claire v16.6-v16.10 Package Update Governance Installer'
INSTALLER_VERSION = '16.6-16.10'
DEFAULT_TARGET = Path.home() / "OneDrive" / "Desktop" / "Claire"

FOLDERS = [
  "src/claire/package_update_governance",
  "tests/package_update_governance",
  "requirements",
  "data/package_update_governance/dependency_snapshots",
  "data/package_update_governance/audit_reports",
  "data/package_update_governance/update_plans",
  "data/package_update_governance/rollback_notes",
  "docs/package_update_governance",
  ".claire_install/reports"
]
FILES = {
  "src/claire/package_update_governance/__init__.py": "\"\"\"Claire package update governance package.\"\"\"\n",
  "src/claire/package_update_governance/dependency_snapshot.py": "from __future__ import annotations\nimport json\nimport subprocess\nimport sys\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any, Dict\n\nclass DependencySnapshot:\n    def __init__(self, root: str | Path = \".\") -> None:\n        self.root = Path(root)\n\n    def build_snapshot(self) -> Dict[str, Any]:\n        try:\n            proc = subprocess.run([sys.executable, \"-m\", \"pip\", \"freeze\"], text=True, capture_output=True, timeout=60)\n            packages = [line.strip() for line in proc.stdout.splitlines() if line.strip()]\n            status = \"success\" if proc.returncode == 0 else \"failed\"\n        except Exception as exc:\n            packages = []\n            status = \"failed\"\n            proc = type(\"Proc\", (), {\"stderr\": str(exc)})()\n        return {\n            \"record_type\": \"dependency_snapshot\",\n            \"status\": status,\n            \"package_count\": len(packages),\n            \"packages\": packages,\n            \"error\": getattr(proc, \"stderr\", \"\"),\n            \"created_at_utc\": datetime.now(timezone.utc).isoformat(),\n        }\n\n    def export_snapshot(self, snapshot: Dict[str, Any]) -> Path:\n        out = self.root / \"data\" / \"package_update_governance\" / \"dependency_snapshots\" / \"dependency_snapshot.json\"\n        out.parent.mkdir(parents=True, exist_ok=True)\n        out.write_text(json.dumps(snapshot, indent=2, sort_keys=True), encoding=\"utf-8\")\n        return out\n",
  "src/claire/package_update_governance/package_update_policy.py": "from __future__ import annotations\nfrom typing import Any, Dict, List\n\nclass PackageUpdatePolicy:\n    def build_policy(self) -> Dict[str, Any]:\n        return {\n            \"record_type\": \"package_update_policy\",\n            \"rule\": \"No blind updates. Snapshot, audit, test, rollback point, then commit.\",\n            \"required_steps\": [\"dependency_snapshot\", \"pip_audit\", \"targeted_pytest\", \"rollback_point\", \"git_commit\"],\n            \"default_update_decision\": \"manual_review_required\",\n        }\n\n    def evaluate(self, completed_steps: List[str]) -> Dict[str, Any]:\n        required = self.build_policy()[\"required_steps\"]\n        missing = [step for step in required if step not in completed_steps]\n        return {\"status\": \"ready\" if not missing else \"blocked\", \"missing\": missing}\n",
  "src/claire/package_update_governance/pip_audit_runner.py": "from __future__ import annotations\nimport json\nimport subprocess\nimport sys\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any, Dict\n\nclass PipAuditRunner:\n    def __init__(self, root: str | Path = \".\") -> None:\n        self.root = Path(root)\n\n    def run_audit(self) -> Dict[str, Any]:\n        try:\n            proc = subprocess.run([sys.executable, \"-m\", \"pip_audit\"], text=True, capture_output=True, timeout=180)\n            return {\n                \"record_type\": \"pip_audit_report\",\n                \"status\": \"success\" if proc.returncode == 0 else \"attention\",\n                \"returncode\": proc.returncode,\n                \"stdout\": proc.stdout,\n                \"stderr\": proc.stderr,\n                \"created_at_utc\": datetime.now(timezone.utc).isoformat(),\n            }\n        except Exception as exc:\n            return {\n                \"record_type\": \"pip_audit_report\",\n                \"status\": \"not_available\",\n                \"error\": str(exc),\n                \"hint\": \"Install with: python -m pip install pip-audit\",\n                \"created_at_utc\": datetime.now(timezone.utc).isoformat(),\n            }\n\n    def export_report(self, report: Dict[str, Any]) -> Path:\n        out = self.root / \"data\" / \"package_update_governance\" / \"audit_reports\" / \"pip_audit_report.json\"\n        out.parent.mkdir(parents=True, exist_ok=True)\n        out.write_text(json.dumps(report, indent=2, sort_keys=True), encoding=\"utf-8\")\n        return out\n",
  "src/claire/package_update_governance/update_plan_builder.py": "from __future__ import annotations\nfrom typing import Any, Dict, List\n\nclass UpdatePlanBuilder:\n    def build_plan(self, packages: List[str], reason: str) -> Dict[str, Any]:\n        return {\n            \"record_type\": \"package_update_plan\",\n            \"packages\": packages,\n            \"reason\": reason,\n            \"required_validation\": [\"pip_audit\", \"pytest\", \"launcher_test\", \"dashboard_test\"],\n            \"rollback_required\": True,\n        }\n",
  "requirements/live_connectivity.in": "httpx\nrequests\npip-audit\npip-tools\n",
  "docs/package_update_governance/v16_6_to_v16_10_package_update_plan.md": "# v16.6-v16.10 Internet Package Update Governance\n\nRecommended modern package approach:\n\n1. Keep direct dependency intent in `requirements/*.in`.\n2. Compile pinned requirements with `pip-compile`.\n3. Audit with `pip-audit`.\n4. Run targeted and full pytest.\n5. Commit only after launcher/dashboard proof.\n\nManual commands:\n\n```bat\npython -m pip install pip-tools pip-audit\npip-compile requirements/live_connectivity.in -o requirements/live_connectivity.txt\npython -m pip install -r requirements/live_connectivity.txt\npython -m pip_audit\npytest\n```\n",
  "tests/package_update_governance/test_package_update_governance.py": "from claire.package_update_governance.package_update_policy import PackageUpdatePolicy\nfrom claire.package_update_governance.update_plan_builder import UpdatePlanBuilder\n\ndef test_policy_blocks_missing_steps():\n    assert PackageUpdatePolicy().evaluate([])[\"status\"] == \"blocked\"\n\ndef test_update_plan_requires_rollback():\n    assert UpdatePlanBuilder().build_plan([\"httpx\"], \"live connectivity\")[\"rollback_required\"] is True\n"
}
VERSION_FILE = 'version_16_10_package_update_governance.json'
LOCK_PAYLOAD = {
  "version": "16.10",
  "state": "package_update_governance_installed",
  "phase": "internet_package_updates",
  "status": "manual_review_required",
  "safety_contract": {
    "does_not_modify_runtime": True,
    "does_not_modify_routing": True,
    "does_not_modify_scoring": True,
    "does_not_modify_core_run_output": True,
    "live_data_gated": True,
    "internet_access_requires_allowlist": True
  }
}

def stamp() -> str:
    return datetime.now(timezone.utc).strftime("%Y%m%d_%H%M%S")

def now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()

def ensure_dir(path: Path, install: bool, actions: List[Dict[str, Any]]) -> None:
    existed = path.exists()
    if install:
        path.mkdir(parents=True, exist_ok=True)
    actions.append({"action": "ensure_dir", "path": str(path), "installed": install, "existed": existed})

def write_file(path: Path, content: str, install: bool, actions: List[Dict[str, Any]]) -> None:
    existed = path.exists()
    backup = None
    if install:
        path.parent.mkdir(parents=True, exist_ok=True)
        if existed:
            backup_path = path.with_name(path.name + ".bak_" + stamp())
            shutil.copy2(path, backup_path)
            backup = str(backup_path)
        path.write_text(content, encoding="utf-8", newline="\n")
    actions.append({"action": "write_file", "path": str(path), "installed": install, "existed": existed, "backup": backup})

def main() -> int:
    parser = argparse.ArgumentParser(description=INSTALLER_NAME)
    parser.add_argument("--target", default=str(DEFAULT_TARGET), help="Claire project root")
    parser.add_argument("--install", action="store_true", help="Apply install. Without this, dry run only.")
    args = parser.parse_args()

    root = Path(args.target).expanduser().resolve()
    install = bool(args.install)

    print(f"{INSTALLER_NAME} {INSTALLER_VERSION}")
    print(f"Target: {root}")
    print("Mode:", "INSTALL" if install else "DRY RUN")

    if not root.exists():
        print(f"[ERROR] Target does not exist: {root}")
        return 2
    if not (root / "src").exists():
        print("[ERROR] This does not look like Claire root. Missing src folder.")
        return 3

    actions: List[Dict[str, Any]] = []
    for folder in FOLDERS:
        ensure_dir(root / folder, install, actions)
    for rel, content in FILES.items():
        write_file(root / rel, content, install, actions)

    payload = dict(LOCK_PAYLOAD)
    payload["installed_at_utc"] = now_iso()
    write_file(root / VERSION_FILE, json.dumps(payload, indent=2, sort_keys=True), install, actions)

    report = {
        "installer": INSTALLER_NAME,
        "installer_version": INSTALLER_VERSION,
        "installed": install,
        "installed_at_utc": now_iso(),
        "target": str(root),
        "actions": actions,
        "version_file": VERSION_FILE,
        "safety_contract": payload.get("safety_contract", {}),
        "post_install_checks": [
            "Run the targeted pytest folder for this installer.",
            "Review docs before enabling live internet use.",
            "Do not add live data to high-confidence scoring without lineage and source policy approval.",
            "Commit after tests pass."
        ]
    }

    if install:
        report_path = root / ".claire_install" / "reports" / (VERSION_FILE.replace(".json", "") + "_" + stamp() + ".json")
        report_path.parent.mkdir(parents=True, exist_ok=True)
        report_path.write_text(json.dumps(report, indent=2, sort_keys=True), encoding="utf-8")
        print(f"[OK] Report written: {report_path}")
    else:
        print("[DRY RUN] No files changed. Re-run with --install to apply.")

    print(json.dumps({
        "installed": install,
        "target": str(root),
        "installer_version": INSTALLER_VERSION,
        "folders": len(FOLDERS),
        "files": len(FILES) + 1
    }, indent=2))
    return 0

if __name__ == "__main__":
    raise SystemExit(main())

