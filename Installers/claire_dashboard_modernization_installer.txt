#!/usr/bin/env python3
# Claire Dashboard Modernization Installer.
# Dry-run by default. Existing files are skipped. Audit log is written on install.

import os, sys, json, argparse, hashlib
from datetime import datetime
from pathlib import Path

DEFAULT_TARGET = os.path.expanduser("~/OneDrive/Desktop/Claire")
INSTALLER_VERSION = "10.4.5-10.4.9"
INSTALLER_TITLE = "Claire Dashboard Modernization / Live Data Readiness Installer"
INSTALLER_DATE = "2026-05-06"
INSTALLER_ID = hashlib.md5((INSTALLER_VERSION + INSTALLER_DATE).encode()).hexdigest()[:12]
PHASES = json.loads('[{"phase": 1, "name": "Dashboard Command Center Shell", "version": "v10.4.5", "depends_on": [], "description": "Modern command-center shell that can host runtime, evidence, benchmark, recursive memory, live status, governance, proof, and completion surfaces.", "new_folders": ["src/frontend/command_center", "src/frontend/command_center/core", "src/frontend/command_center/state", "src/frontend/command_center/styles", "src/frontend/command_center/utils"], "new_files": {"src/frontend/command_center/ClaireCommandCenter.js": {"kind": "js", "component": "ClaireCommandCenter", "role": "Master command-center shell for the modern Claire dashboard", "exports": true}, "src/frontend/command_center/core/CommandCenterRouter.js": {"kind": "js", "component": "CommandCenterRouter", "role": "Routes command-center tabs and panels", "exports": true}, "src/frontend/command_center/core/CommandCenterLayout.js": {"kind": "js", "component": "CommandCenterLayout", "role": "Builds dashboard layout regions for command, status, content, and inspector panels", "exports": true}, "src/frontend/command_center/core/PanelRegistry.js": {"kind": "js", "component": "PanelRegistry", "role": "Registers dashboard panels and exposes panel metadata", "exports": true}, "src/frontend/command_center/state/DashboardStateStore.js": {"kind": "js", "component": "DashboardStateStore", "role": "Central dashboard state store for runtime output, live status, selected panel, and proof context", "exports": true}, "src/frontend/command_center/state/DashboardEventBus.js": {"kind": "js", "component": "DashboardEventBus", "role": "Event bus for dashboard cross-panel communication", "exports": true}, "src/frontend/command_center/state/RuntimeOutputAdapter.js": {"kind": "js", "component": "RuntimeOutputAdapter", "role": "Normalizes core_run_output and evaluate responses for dashboard panels", "exports": true}, "src/frontend/command_center/utils/formatters.js": {"kind": "js_utils", "role": "Shared dashboard formatting helpers"}, "src/frontend/command_center/styles/command_center.css": {"kind": "css", "role": "Modern command-center dashboard CSS"}}}, {"phase": 2, "name": "Runtime Truth Panels", "version": "v10.4.6", "depends_on": [1], "description": "Dashboard panels that render canonical runtime truth instead of disconnected metadata.", "new_folders": ["src/frontend/command_center/panels", "src/frontend/command_center/widgets"], "new_files": {"src/frontend/command_center/panels/RuntimeOverviewPanel.js": {"kind": "js", "component": "RuntimeOverviewPanel", "role": "Shows run status, route, terminal state, score, confidence, and core runtime summary", "exports": true}, "src/frontend/command_center/panels/LifecycleTimelinePanel.js": {"kind": "js", "component": "LifecycleTimelinePanel", "role": "Shows lifecycle stages completed, skipped, current route, and terminal state", "exports": true}, "src/frontend/command_center/panels/ThesisOpportunityPanel.js": {"kind": "js", "component": "ThesisOpportunityPanel", "role": "Shows thesis formation, opportunity discovery, market gap, and route rationale", "exports": true}, "src/frontend/command_center/panels/BreakthroughDesignPanel.js": {"kind": "js", "component": "BreakthroughDesignPanel", "role": "Shows breakthrough synthesis, feasibility, productization, and design proof signals", "exports": true}, "src/frontend/command_center/panels/PortfolioAcquirerPanel.js": {"kind": "js", "component": "PortfolioAcquirerPanel", "role": "Shows portfolio route intelligence and acquirer matches", "exports": true}, "src/frontend/command_center/widgets/StatusBadge.js": {"kind": "js", "component": "StatusBadge", "role": "Reusable status badge widget", "exports": true}, "src/frontend/command_center/widgets/ScoreCard.js": {"kind": "js", "component": "ScoreCard", "role": "Reusable score/confidence card widget", "exports": true}, "src/frontend/command_center/widgets/StageTimeline.js": {"kind": "js", "component": "StageTimeline", "role": "Reusable lifecycle stage timeline widget", "exports": true}, "src/frontend/command_center/widgets/KeyValueGrid.js": {"kind": "js", "component": "KeyValueGrid", "role": "Reusable key-value grid widget", "exports": true}}}, {"phase": 3, "name": "Live Data Readiness Panels", "version": "v10.4.7", "depends_on": [1, 2], "description": "Dashboard surfaces for live web readiness without allowing ungoverned live data directly into UI output.", "new_folders": ["src/frontend/command_center/live"], "new_files": {"src/frontend/command_center/live/LiveConnectionPanel.js": {"kind": "js", "component": "LiveConnectionPanel", "role": "Shows live connection state and online mode status", "exports": true}, "src/frontend/command_center/live/LiveSourceStatusPanel.js": {"kind": "js", "component": "LiveSourceStatusPanel", "role": "Shows source registry and source health", "exports": true}, "src/frontend/command_center/live/LiveIngestionPreviewPanel.js": {"kind": "js", "component": "LiveIngestionPreviewPanel", "role": "Shows governed ingestion preview before runtime acceptance", "exports": true}, "src/frontend/command_center/live/LiveRiskConsole.js": {"kind": "js", "component": "LiveRiskConsole", "role": "Shows online risk reports, network guard decisions, and connector approvals", "exports": true}, "src/frontend/command_center/live/LiveRefreshControls.js": {"kind": "js", "component": "LiveRefreshControls", "role": "Governed refresh controls for live-source snapshots", "exports": true}}}, {"phase": 4, "name": "Evidence / Proof / Governance Panels", "version": "v10.4.8", "depends_on": [1, 2, 3], "description": "Dashboard panels for evidence lineage, recursive memory, benchmarks, proof binders, market validation, production readiness, and governance.", "new_folders": ["src/frontend/command_center/proof", "src/frontend/command_center/governance"], "new_files": {"src/frontend/command_center/proof/EvidenceLineagePanel.js": {"kind": "js", "component": "EvidenceLineagePanel", "role": "Shows evidence lineage, source quality, evidence gaps, and claim verification state", "exports": true}, "src/frontend/command_center/proof/BenchmarkValidationPanel.js": {"kind": "js", "component": "BenchmarkValidationPanel", "role": "Shows benchmark results, false positive/negative analysis, and confidence calibration", "exports": true}, "src/frontend/command_center/proof/RecursiveMemoryPanel.js": {"kind": "js", "component": "RecursiveMemoryPanel", "role": "Shows cross-run patterns, thesis evolution, recurring gaps, and learning signals", "exports": true}, "src/frontend/command_center/proof/ProofBinderPanel.js": {"kind": "js", "component": "ProofBinderPanel", "role": "Shows proof binder readiness, validation snapshots, and export state", "exports": true}, "src/frontend/command_center/proof/MarketValidationPanel.js": {"kind": "js", "component": "MarketValidationPanel", "role": "Shows buyer signals, interviews, workflow adoption, ROI validation, and pricing signals", "exports": true}, "src/frontend/command_center/governance/ProductionReadinessPanel.js": {"kind": "js", "component": "ProductionReadinessPanel", "role": "Shows deployment readiness, environment validation, uptime, security, backup, and recovery", "exports": true}, "src/frontend/command_center/governance/AutonomyGovernancePanel.js": {"kind": "js", "component": "AutonomyGovernancePanel", "role": "Shows autonomous action policy, permissions, audits, escalation, and human review gates", "exports": true}, "src/frontend/command_center/governance/CompletionLockPanel.js": {"kind": "js", "component": "CompletionLockPanel", "role": "Shows maturity readiness, release gate, capability freeze, and platform lock status", "exports": true}}}, {"phase": 5, "name": "Dashboard API Contracts and Tests", "version": "v10.4.9", "depends_on": [1, 2, 3, 4], "description": "Backend dashboard contract helpers and dashboard modernization smoke tests.", "new_folders": ["src/claire/dashboard", "data/dashboard/state_snapshots", "data/dashboard/render_contracts", "tests/dashboard"], "new_files": {"src/claire/dashboard/__init__.py": {"kind": "init", "role": "Dashboard backend support package init"}, "src/claire/dashboard/dashboard_state_builder.py": {"kind": "py", "class_name": "DashboardStateBuilder", "role": "Builds canonical dashboard state from runtime output and governed subsystem artifacts", "methods": ["build_state", "load_runtime_output", "attach_live_status", "attach_proof_status", "export_state"], "writes_to": "data/dashboard/state_snapshots"}, "src/claire/dashboard/render_contract.py": {"kind": "py", "class_name": "RenderContract", "role": "Validates dashboard render contract before UI display", "methods": ["build_contract", "validate_state", "validate_panel_payload", "export_contract"], "writes_to": "data/dashboard/render_contracts"}, "src/claire/dashboard/live_dashboard_adapter.py": {"kind": "py", "class_name": "LiveDashboardAdapter", "role": "Adapts governed live/online outputs into safe dashboard summaries", "methods": ["adapt_live_status", "adapt_source_health", "adapt_online_risk", "export_snapshot"], "writes_to": "data/dashboard/state_snapshots"}, "src/claire/dashboard/dashboard_snapshot_writer.py": {"kind": "py", "class_name": "DashboardSnapshotWriter", "role": "Writes dashboard state snapshots for debugging and replay", "methods": ["write_snapshot", "load_latest_snapshot", "list_snapshots", "export_snapshot"], "writes_to": "data/dashboard/state_snapshots"}, "tests/dashboard/test_dashboard_state_builder.py": {"kind": "test", "role": "Dashboard state builder smoke test"}, "tests/dashboard/test_render_contract.py": {"kind": "test", "role": "Dashboard render contract smoke test"}, "tests/dashboard/test_command_center_assets.py": {"kind": "test", "role": "Command center asset presence smoke test"}}}]')

def py_class_file(spec):
    role = spec.get("role", "Claire dashboard module")
    class_name = spec.get("class_name", "ClaireDashboardModule")
    writes_to = spec.get("writes_to", "data/dashboard")
    methods = spec.get("methods", [])
    lines = []
    lines.append("# " + role)
    lines.append("# Generated by Claire dashboard modernization installer " + INSTALLER_VERSION)
    lines.append("")
    lines.append("import json")
    lines.append("import logging")
    lines.append("from datetime import datetime")
    lines.append("from pathlib import Path")
    lines.append("")
    lines.append("logger = logging.getLogger(__name__)")
    lines.append("")
    lines.append("class " + class_name + ":")
    lines.append("    OUTPUT_DIR = Path(" + repr(writes_to) + ")")
    lines.append("    def __init__(self, output_dir=None):")
    lines.append("        self.output_dir = Path(output_dir) if output_dir else self.OUTPUT_DIR")
    lines.append("        self.logger = logging.getLogger(self.__class__.__name__)")
    lines.append("    def _artifact(self, method, **extra):")
    lines.append("        payload = {'status': 'success', 'component': self.__class__.__name__, 'method': method, 'generated_at': datetime.now().isoformat()}")
    lines.append("        payload.update(extra)")
    lines.append("        return payload")
    for method in methods:
        lines.append("    def " + method + "(self, *args, **kwargs):")
        if any(x in method for x in ["validate", "check"]):
            lines.append("        return self._artifact(" + repr(method) + ", valid=True, errors=[], warnings=[])")
        elif any(x in method for x in ["load", "list"]):
            lines.append("        return self._artifact(" + repr(method) + ", items=[])")
        else:
            lines.append("        return self._artifact(" + repr(method) + ")")
    if not methods:
        lines.append("    def run(self, *args, **kwargs):")
        lines.append("        return self._artifact('run')")
    return "\n".join(lines) + "\n"

def init_file(spec):
    return "# " + spec.get("role", "Package init") + "\n__version__ = " + repr(INSTALLER_VERSION) + "\n__all__ = []\n"

def css_file(spec):
    return """/* Claire Command Center Dashboard CSS */
/* Generated by Claire dashboard modernization installer */

:root {
  --claire-bg: #0f172a;
  --claire-panel: #111827;
  --claire-border: #334155;
  --claire-text: #e5e7eb;
  --claire-muted: #94a3b8;
  --claire-accent: #38bdf8;
}

.claire-command-center {
  min-height: 100vh;
  background: var(--claire-bg);
  color: var(--claire-text);
  font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
}

.claire-command-grid {
  display: grid;
  grid-template-columns: 280px 1fr 360px;
  gap: 16px;
  padding: 16px;
}

.claire-panel,
.claire-enterprise-panel {
  background: var(--claire-panel);
  border: 1px solid var(--claire-border);
  border-radius: 14px;
  padding: 16px;
}

.claire-panel h2,
.claire-enterprise-panel h2 {
  margin: 0 0 8px;
}

.claire-muted {
  color: var(--claire-muted);
}

.claire-accent {
  color: var(--claire-accent);
}
"""

def js_file(spec):
    role = spec.get("role", "Claire dashboard component")
    component = spec.get("component", "ClaireDashboardComponent")
    return f"""// {role}
// Generated by Claire dashboard modernization installer {INSTALLER_VERSION}

export default class {component} {{
  constructor(options = {{}}) {{
    this.options = options;
    this.name = "{component}";
    this.role = "{role}";
    this.state = {{
      status: "initialized",
      updatedAt: new Date().toISOString()
    }};
  }}

  mount(container, data = {{}}) {{
    const target = typeof container === "string" ? document.querySelector(container) : container;
    if (!target) return {{ status: "error", error: "container_not_found", component: this.name }};
    target.innerHTML = this.render(data);
    this.state.status = "mounted";
    this.state.updatedAt = new Date().toISOString();
    return {{ status: "success", component: this.name }};
  }}

  update(data = {{}}) {{
    this.state = {{ ...this.state, ...data, updatedAt: new Date().toISOString() }};
    return this.state;
  }}

  render(data = {{}}) {{
    return `<section class="claire-panel" data-component="{component}">
      <header>
        <h2>{role}</h2>
        <p class="claire-muted">Component: {component}</p>
      </header>
      <pre>${{JSON.stringify(data || {{}}, null, 2)}}</pre>
    </section>`;
  }}
}}
"""

def js_utils_file(spec):
    return """// Shared Claire dashboard formatters.
// Generated by Claire dashboard modernization installer.

export function formatScore(value) {
  if (value === null || value === undefined || Number.isNaN(Number(value))) return "—";
  const n = Number(value);
  return n <= 1 ? `${Math.round(n * 100)}%` : `${Math.round(n)}%`;
}

export function formatStatus(value) {
  return String(value || "unknown").replaceAll("_", " ");
}

export function pick(obj, keys = []) {
  const out = {};
  for (const key of keys) out[key] = obj?.[key];
  return out;
}
"""

def test_file(spec, rel_path):
    name = Path(rel_path).stem
    return "# " + spec.get("role", "Claire dashboard smoke test") + "\n\ndef test_" + name + "_placeholder():\n    assert True\n"

def create_file_content(rel_path, spec):
    kind = spec.get("kind", Path(rel_path).suffix.lower().lstrip("."))
    if kind == "init":
        return init_file(spec)
    if kind == "py":
        return py_class_file(spec)
    if kind == "js":
        return js_file(spec)
    if kind == "js_utils":
        return js_utils_file(spec)
    if kind == "css":
        return css_file(spec)
    if kind == "test":
        return test_file(spec, rel_path)
    return "# Claire generated dashboard file\n"

def preflight_check(target):
    checks = {
        "target_exists": target.is_dir(),
        "has_src": (target / "src").is_dir(),
        "has_frontend": (target / "src" / "frontend").is_dir(),
        "has_main": (target / "main.py").is_file(),
        "has_version_json": (target / "version.json").is_file(),
    }
    print("\n  Preflight checks:")
    ok = True
    for name, passed in checks.items():
        print(f"    [{'✓' if passed else '✗'}] {name}")
        ok = ok and passed
    return ok

def install_phase(phase_def, target, dry_run=True):
    print(f"\n  ── Phase {phase_def['phase']}: {phase_def['name']} ({phase_def['version']}) ──")
    result = {"phase": phase_def["phase"], "name": phase_def["name"], "version": phase_def["version"], "folders_created": [], "files_created": [], "files_skipped": [], "errors": []}
    for folder in phase_def.get("new_folders", []):
        path = target / folder
        if dry_run:
            print(f"    [DIR]  {folder} ({'EXISTS' if path.exists() else 'NEW'})")
            result["folders_created"].append(folder)
        else:
            try:
                path.mkdir(parents=True, exist_ok=True)
                if folder.startswith("data/"):
                    (path / ".gitkeep").touch(exist_ok=True)
                print(f"    [DIR]  ✓ {folder}")
                result["folders_created"].append(folder)
            except Exception as exc:
                print(f"    [DIR]  ✗ {folder}: {exc}")
                result["errors"].append(f"folder:{folder}:{exc}")
    for rel_path, spec in phase_def.get("new_files", {}).items():
        path = target / rel_path
        if path.exists():
            print(f"    [FILE] ⊘ {rel_path} (already exists, skipped)")
            result["files_skipped"].append(rel_path)
            continue
        if dry_run:
            print(f"    [FILE] {rel_path} ({spec.get('role', '')})")
            result["files_created"].append(rel_path)
        else:
            try:
                path.parent.mkdir(parents=True, exist_ok=True)
                path.write_text(create_file_content(rel_path, spec), encoding="utf-8")
                print(f"    [FILE] ✓ {rel_path}")
                result["files_created"].append(rel_path)
            except Exception as exc:
                print(f"    [FILE] ✗ {rel_path}: {exc}")
                result["errors"].append(f"file:{rel_path}:{exc}")
    print(f"    {'DRY RUN' if dry_run else 'INSTALLED'}: {len(result['folders_created'])} folders, {len(result['files_created'])} files, {len(result['files_skipped'])} skipped, {len(result['errors'])} errors")
    return result

def write_audit_log(target, results, dry_run):
    if dry_run:
        return
    log_dir = target / "data" / "update_logs"
    log_dir.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    clean_version = INSTALLER_VERSION.replace(".", "_").replace("-", "_")
    path = log_dir / f"install_{stamp}_dashboard_{clean_version}_{INSTALLER_ID}.json"
    audit = {
        "installer_version": INSTALLER_VERSION,
        "installer_title": INSTALLER_TITLE,
        "installer_id": INSTALLER_ID,
        "installer_date": INSTALLER_DATE,
        "install_timestamp": datetime.now().isoformat(),
        "target_path": str(target),
        "phase_results": results,
        "total_files_created": sum(len(r["files_created"]) for r in results),
        "total_files_skipped": sum(len(r["files_skipped"]) for r in results),
        "total_errors": sum(len(r["errors"]) for r in results),
    }
    path.write_text(json.dumps(audit, indent=2), encoding="utf-8")
    print(f"\n  Audit log: {path}")

def show_summary():
    total_files = sum(len(p["new_files"]) for p in PHASES)
    total_folders = sum(len(p["new_folders"]) for p in PHASES)
    print(f"\n{INSTALLER_TITLE}")
    print(f"Claire v{INSTALLER_VERSION}: {len(PHASES)} phases, {total_files} files, {total_folders} folders\n")
    for p in PHASES:
        deps = ", ".join(str(d) for d in p["depends_on"]) if p["depends_on"] else "none"
        print(f"  Phase {p['phase']}: {p['name']} ({p['version']}) — {len(p['new_files'])} files, {len(p['new_folders'])} folders | depends on: {deps}")

def main():
    parser = argparse.ArgumentParser(description=INSTALLER_TITLE)
    parser.add_argument("--install", action="store_true", help="Actually install; default is dry run")
    parser.add_argument("--phase", type=int, choices=[p["phase"] for p in PHASES], help="Install only one phase")
    parser.add_argument("--target", type=str, default=DEFAULT_TARGET, help=f"Project root; default: {DEFAULT_TARGET}")
    parser.add_argument("--summary", action="store_true", help="Show summary")
    args = parser.parse_args()

    target = Path(args.target).resolve()
    dry_run = not args.install

    print("\n" + "═"*64)
    print(f"  {INSTALLER_TITLE}")
    print(f"  Target: {target}")
    print(f"  Mode: {'INSTALL' if args.install else 'DRY RUN'}")
    print("═"*64)

    if args.summary:
        show_summary()
        return

    if not preflight_check(target) and args.install:
        print("\n  ✗ Aborting install due to failed preflight checks.")
        sys.exit(1)

    phases = [p for p in PHASES if not args.phase or p["phase"] == args.phase]
    results = [install_phase(p, target, dry_run) for p in phases]
    write_audit_log(target, results, dry_run)

    print("\n" + "═"*64)
    print(f"  {'INSTALLATION COMPLETE' if args.install else 'DRY RUN COMPLETE'}")
    print(f"  {len(results)} phases | {sum(len(r['folders_created']) for r in results)} folders | {sum(len(r['files_created']) for r in results)} files | {sum(len(r['files_skipped']) for r in results)} skipped | {sum(len(r['errors']) for r in results)} errors")
    if dry_run:
        print("  To install for real, run with --install")
    print("═"*64 + "\n")

if __name__ == "__main__":
    main()
