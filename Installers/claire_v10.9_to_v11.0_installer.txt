#!/usr/bin/env python3
# Claire governed additive installer.
# Dry-run by default. Existing files are skipped. Audit log is written on install.

import os, sys, json, argparse, hashlib
from datetime import datetime
from pathlib import Path

DEFAULT_TARGET = os.path.expanduser("~/OneDrive/Desktop/Claire")
INSTALLER_VERSION = "10.9-11.0"
INSTALLER_TITLE = "Claire v10.9 to v11.0 Audit / Proof / Maturation / Platform Lock Installer"
INSTALLER_DATE = "2026-05-06"
INSTALLER_ID = hashlib.md5((INSTALLER_VERSION + INSTALLER_DATE).encode()).hexdigest()[:12]
PHASES = json.loads('[{"phase": 1, "name": "System Audit & Consolidation", "version": "v10.9", "depends_on": [], "new_folders": ["src/claire/audit", "data/audits/system_integrity", "data/audits/module_overlap", "data/audits/unused_files", "data/audits/import_health", "data/audits/lifecycle_coherence"], "new_files": {"src/claire/audit/__init__.py": {"kind": "init", "role": "Audit package init"}, "src/claire/audit/system_integrity_auditor.py": {"kind": "py", "class_name": "SystemIntegrityAuditor", "role": "Audits system integrity", "methods": ["audit_system", "check_required_roots", "check_version_files", "export_audit"], "writes_to": "data/audits/system_integrity"}, "src/claire/audit/module_overlap_detector.py": {"kind": "py", "class_name": "ModuleOverlapDetector", "role": "Detects overlapping modules", "methods": ["detect_overlap", "group_similar_modules", "score_overlap", "export_report"], "writes_to": "data/audits/module_overlap"}, "src/claire/audit/unused_file_detector.py": {"kind": "py", "class_name": "UnusedFileDetector", "role": "Detects likely unused files", "methods": ["detect_unused_files", "classify_file", "recommend_action", "export_report"], "writes_to": "data/audits/unused_files"}, "src/claire/audit/duplicate_route_detector.py": {"kind": "py", "class_name": "DuplicateRouteDetector", "role": "Detects duplicate routes", "methods": ["detect_duplicate_routes", "extract_routes", "score_conflicts", "export_report"], "writes_to": "data/audits/system_integrity"}, "src/claire/audit/import_health_checker.py": {"kind": "py", "class_name": "ImportHealthChecker", "role": "Checks import health", "methods": ["check_import_health", "check_module_import", "summarize_failures", "export_report"], "writes_to": "data/audits/import_health"}, "src/claire/audit/lifecycle_coherence_auditor.py": {"kind": "py", "class_name": "LifecycleCoherenceAuditor", "role": "Audits lifecycle coherence", "methods": ["audit_lifecycle", "check_stage_contracts", "check_terminal_state", "export_report"], "writes_to": "data/audits/lifecycle_coherence"}, "src/claire/audit/audit_report_builder.py": {"kind": "py", "class_name": "AuditReportBuilder", "role": "Builds consolidated audit reports", "methods": ["build_report", "summarize_findings", "format_markdown", "export_report"], "writes_to": "data/audits/system_integrity"}}}, {"phase": 2, "name": "Runtime Proof Binder", "version": "v10.10", "depends_on": [1], "new_folders": ["src/claire/proof", "data/proof/binders", "data/proof/runtime_snapshots", "data/proof/contract_passes", "data/proof/validation_snapshots", "data/proof/evidence_traces"], "new_files": {"src/claire/proof/__init__.py": {"kind": "init", "role": "Proof package init"}, "src/claire/proof/proof_binder.py": {"kind": "py", "class_name": "ProofBinder", "role": "Builds runtime proof binders", "methods": ["build_binder", "attach_evidence_trace", "validate_binder", "export_binder"], "writes_to": "data/proof/binders"}, "src/claire/proof/runtime_proof_collector.py": {"kind": "py", "class_name": "RuntimeProofCollector", "role": "Collects runtime proof artifacts", "methods": ["collect_runtime_proof", "collect_latest_output", "collect_logs", "export_collection"], "writes_to": "data/proof/runtime_snapshots"}, "src/claire/proof/evidence_trace_builder.py": {"kind": "py", "class_name": "EvidenceTraceBuilder", "role": "Builds evidence traces", "methods": ["build_trace", "trace_evidence_item", "validate_trace", "export_trace"], "writes_to": "data/proof/evidence_traces"}, "src/claire/proof/contract_pass_registry.py": {"kind": "py", "class_name": "ContractPassRegistry", "role": "Registers contract validation passes", "methods": ["register_pass", "register_failure", "load_registry", "export_registry"], "writes_to": "data/proof/contract_passes"}, "src/claire/proof/validation_snapshotter.py": {"kind": "py", "class_name": "ValidationSnapshotter", "role": "Captures validation snapshots", "methods": ["capture_snapshot", "compare_snapshots", "summarize_snapshot", "export_snapshot"], "writes_to": "data/proof/validation_snapshots"}, "src/claire/proof/proof_exporter.py": {"kind": "py", "class_name": "ProofExporter", "role": "Exports proof artifacts", "methods": ["export_json", "export_markdown", "bundle_exports", "validate_export"], "writes_to": "data/proof/binders"}}}, {"phase": 3, "name": "Maturation Validation", "version": "v10.11", "depends_on": [1, 2], "new_folders": ["src/claire/maturation", "data/maturation/scorecards", "data/maturation/readiness_reports", "data/maturation/regression_rechecks", "data/maturation/completion_audits"], "new_files": {"src/claire/maturation/__init__.py": {"kind": "init", "role": "Maturation package init"}, "src/claire/maturation/maturity_scorecard.py": {"kind": "py", "class_name": "MaturityScorecard", "role": "Builds maturation scorecards", "methods": ["score_maturity", "score_dimension", "summarize_scorecard", "export_scorecard"], "writes_to": "data/maturation/scorecards"}, "src/claire/maturation/readiness_gate.py": {"kind": "py", "class_name": "ReadinessGate", "role": "Evaluates readiness gates", "methods": ["evaluate_gate", "evaluate_all_gates", "list_blockers", "export_report"], "writes_to": "data/maturation/readiness_reports"}, "src/claire/maturation/operational_completion_auditor.py": {"kind": "py", "class_name": "OperationalCompletionAuditor", "role": "Audits operational completion", "methods": ["audit_completion", "check_operational_surfaces", "check_data_outputs", "export_audit"], "writes_to": "data/maturation/completion_audits"}, "src/claire/maturation/regression_recheck_runner.py": {"kind": "py", "class_name": "RegressionRecheckRunner", "role": "Runs regression rechecks", "methods": ["run_rechecks", "summarize_results", "detect_regressions", "export_recheck"], "writes_to": "data/maturation/regression_rechecks"}, "src/claire/maturation/platform_maturity_reporter.py": {"kind": "py", "class_name": "PlatformMaturityReporter", "role": "Reports platform maturity", "methods": ["generate_report", "format_markdown", "format_json", "export_report"], "writes_to": "data/maturation/readiness_reports"}}}, {"phase": 4, "name": "Final v10 Platform Lock", "version": "v10.12", "depends_on": [1, 2, 3], "new_folders": ["src/claire/platform_lock", "data/platform_lock/manifests", "data/platform_lock/capability_freezes", "data/platform_lock/release_gates", "data/platform_lock/final_reports"], "new_files": {"src/claire/platform_lock/__init__.py": {"kind": "init", "role": "Platform lock package init"}, "src/claire/platform_lock/lock_state_writer.py": {"kind": "py", "class_name": "LockStateWriter", "role": "Writes platform lock state", "methods": ["write_lock_state", "load_lock_state", "validate_lock_state", "export_lock_state"], "writes_to": "data/platform_lock/manifests"}, "src/claire/platform_lock/final_state_manifest.py": {"kind": "py", "class_name": "FinalStateManifest", "role": "Builds final state manifest", "methods": ["build_manifest", "attach_capability_summary", "validate_manifest", "export_manifest"], "writes_to": "data/platform_lock/manifests"}, "src/claire/platform_lock/capability_freeze_registry.py": {"kind": "py", "class_name": "CapabilityFreezeRegistry", "role": "Registers frozen capabilities", "methods": ["freeze_capability", "list_frozen_capabilities", "validate_freeze", "export_registry"], "writes_to": "data/platform_lock/capability_freezes"}, "src/claire/platform_lock/release_gate.py": {"kind": "py", "class_name": "ReleaseGate", "role": "Evaluates release gates", "methods": ["evaluate_release", "check_blockers", "recommend_release_state", "export_gate_report"], "writes_to": "data/platform_lock/release_gates"}, "src/claire/platform_lock/v10_completion_report.py": {"kind": "py", "class_name": "V10CompletionReport", "role": "Generates v10 completion report", "methods": ["generate_completion_report", "format_markdown", "format_json", "export_report"], "writes_to": "data/platform_lock/final_reports"}}}, {"phase": 5, "name": "Enterprise / Externalization Layer Scaffold", "version": "v11.0", "depends_on": [1, 2, 3, 4], "new_folders": ["src/claire/enterprise", "data/enterprise/readiness", "data/enterprise/externalization_plans"], "new_files": {"src/claire/enterprise/__init__.py": {"kind": "init", "role": "Enterprise externalization package init"}, "src/claire/enterprise/enterprise_readiness_assessor.py": {"kind": "py", "class_name": "EnterpriseReadinessAssessor", "role": "Assesses enterprise readiness", "methods": ["assess_readiness", "identify_enterprise_gaps", "recommend_next_phase", "export_assessment"], "writes_to": "data/enterprise/readiness"}, "src/claire/enterprise/externalization_plan_builder.py": {"kind": "py", "class_name": "ExternalizationPlanBuilder", "role": "Builds enterprise externalization plans", "methods": ["build_plan", "define_rollout_phases", "define_governance_controls", "export_plan"], "writes_to": "data/enterprise/externalization_plans"}}}]')

def py_class_file(spec):
    role = spec.get("role", "Claire module")
    class_name = spec.get("class_name", "ClaireModule")
    writes_to = spec.get("writes_to", "data")
    methods = spec.get("methods", [])
    lines = []
    lines.append("# " + role)
    lines.append("# Generated by Claire installer " + INSTALLER_VERSION)
    lines.append("")
    lines.append("import json")
    lines.append("import logging")
    lines.append("from datetime import datetime")
    lines.append("from pathlib import Path")
    lines.append("")
    lines.append("logger = logging.getLogger(__name__)")
    lines.append("")
    lines.append("class " + class_name + ":")
    lines.append("    OUTPUT_DIR = Path(" + repr(writes_to) + ")")
    lines.append("    def __init__(self, output_dir=None):")
    lines.append("        self.output_dir = Path(output_dir) if output_dir else self.OUTPUT_DIR")
    lines.append("        self.logger = logging.getLogger(self.__class__.__name__)")
    lines.append("    def _artifact(self, method, **extra):")
    lines.append("        payload = {'status': 'success', 'component': self.__class__.__name__, 'method': method, 'generated_at': datetime.now().isoformat()}")
    lines.append("        payload.update(extra)")
    lines.append("        return payload")
    for method in methods:
        lines.append("    def " + method + "(self, *args, **kwargs):")
        if ("score" in method):
            lines.append("        return self._artifact(" + repr(method) + ", score=0.0, level='unscored')")
        elif any(x in method for x in ["check", "validate", "audit", "evaluate"]):
            lines.append("        return self._artifact(" + repr(method) + ", valid=True, errors=[], warnings=[])")
        else:
            lines.append("        return self._artifact(" + repr(method) + ")")
    if not methods:
        lines.append("    def run(self, *args, **kwargs):")
        lines.append("        return self._artifact('run')")
    return "\n".join(lines) + "\n"

def init_file(spec):
    return "# " + spec.get("role", "Package init") + "\n__version__ = " + repr(INSTALLER_VERSION) + "\n__all__ = []\n"

def js_file(spec):
    role = spec.get("role", "Claire UI component")
    component = spec.get("component", "ClaireComponent")
    return f"""// {role}
// Generated by Claire installer {INSTALLER_VERSION}

export default class {component} {{
  constructor(options = {{}}) {{
    this.options = options;
    this.name = "{component}";
    this.role = "{role}";
    this.state = {{ status: "initialized", updatedAt: new Date().toISOString() }};
  }}

  mount(container, data = {{}}) {{
    const target = typeof container === "string" ? document.querySelector(container) : container;
    if (!target) return {{ status: "error", error: "container_not_found", component: this.name }};
    target.innerHTML = this.render(data);
    this.state.status = "mounted";
    this.state.updatedAt = new Date().toISOString();
    return {{ status: "success", component: this.name }};
  }}

  update(data = {{}}) {{
    this.state = {{ ...this.state, ...data, updatedAt: new Date().toISOString() }};
    return this.state;
  }}

  render(data = {{}}) {{
    return `<section class="claire-enterprise-panel" data-component="{component}">
      <header><h2>{role}</h2><p>Status: ${{data.status || this.state.status}}</p></header>
      <pre>${{JSON.stringify(data || {{}}, null, 2)}}</pre>
    </section>`;
  }}
}}
"""

def test_file(spec, rel_path):
    name = Path(rel_path).stem
    return "# " + spec.get("role", "Claire smoke test") + "\n\ndef test_" + name + "_placeholder():\n    assert True\n"

def create_file_content(rel_path, spec):
    kind = spec.get("kind", Path(rel_path).suffix.lower().lstrip("."))
    if kind == "init":
        return init_file(spec)
    if kind == "py":
        return py_class_file(spec)
    if kind == "js":
        return js_file(spec)
    if kind == "test":
        return test_file(spec, rel_path)
    return "# Claire generated file\n"

def preflight_check(target):
    checks = {
        "target_exists": target.is_dir(),
        "has_src": (target / "src").is_dir(),
        "has_main": (target / "main.py").is_file(),
        "has_version_json": (target / "version.json").is_file(),
    }
    print("\n  Preflight checks:")
    ok = True
    for name, passed in checks.items():
        print(f"    [{'✓' if passed else '✗'}] {name}")
        ok = ok and passed
    return ok

def install_phase(phase_def, target, dry_run=True):
    print(f"\n  ── Phase {phase_def['phase']}: {phase_def['name']} ({phase_def['version']}) ──")
    result = {"phase": phase_def["phase"], "name": phase_def["name"], "version": phase_def["version"], "folders_created": [], "files_created": [], "files_skipped": [], "errors": []}
    for folder in phase_def.get("new_folders", []):
        path = target / folder
        if dry_run:
            print(f"    [DIR]  {folder} ({'EXISTS' if path.exists() else 'NEW'})")
            result["folders_created"].append(folder)
        else:
            try:
                path.mkdir(parents=True, exist_ok=True)
                if folder.startswith("data/"):
                    (path / ".gitkeep").touch(exist_ok=True)
                print(f"    [DIR]  ✓ {folder}")
                result["folders_created"].append(folder)
            except Exception as exc:
                print(f"    [DIR]  ✗ {folder}: {exc}")
                result["errors"].append(f"folder:{folder}:{exc}")
    for rel_path, spec in phase_def.get("new_files", {}).items():
        path = target / rel_path
        if path.exists():
            print(f"    [FILE] ⊘ {rel_path} (already exists, skipped)")
            result["files_skipped"].append(rel_path)
            continue
        if dry_run:
            print(f"    [FILE] {rel_path} ({spec.get('role', '')})")
            result["files_created"].append(rel_path)
        else:
            try:
                path.parent.mkdir(parents=True, exist_ok=True)
                path.write_text(create_file_content(rel_path, spec), encoding="utf-8")
                print(f"    [FILE] ✓ {rel_path}")
                result["files_created"].append(rel_path)
            except Exception as exc:
                print(f"    [FILE] ✗ {rel_path}: {exc}")
                result["errors"].append(f"file:{rel_path}:{exc}")
    print(f"    {'DRY RUN' if dry_run else 'INSTALLED'}: {len(result['folders_created'])} folders, {len(result['files_created'])} files, {len(result['files_skipped'])} skipped, {len(result['errors'])} errors")
    return result

def write_audit_log(target, results, dry_run):
    if dry_run:
        return
    log_dir = target / "data" / "update_logs"
    log_dir.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    clean_version = INSTALLER_VERSION.replace(".", "_").replace("-", "_")
    path = log_dir / f"install_{stamp}_{clean_version}_{INSTALLER_ID}.json"
    audit = {
        "installer_version": INSTALLER_VERSION,
        "installer_title": INSTALLER_TITLE,
        "installer_id": INSTALLER_ID,
        "installer_date": INSTALLER_DATE,
        "install_timestamp": datetime.now().isoformat(),
        "target_path": str(target),
        "phase_results": results,
        "total_files_created": sum(len(r["files_created"]) for r in results),
        "total_files_skipped": sum(len(r["files_skipped"]) for r in results),
        "total_errors": sum(len(r["errors"]) for r in results),
    }
    path.write_text(json.dumps(audit, indent=2), encoding="utf-8")
    print(f"\n  Audit log: {path}")

def show_summary():
    total_files = sum(len(p["new_files"]) for p in PHASES)
    total_folders = sum(len(p["new_folders"]) for p in PHASES)
    print(f"\n{INSTALLER_TITLE}")
    print(f"Claire v{INSTALLER_VERSION}: {len(PHASES)} phases, {total_files} files, {total_folders} folders\n")
    for p in PHASES:
        deps = ", ".join(str(d) for d in p["depends_on"]) if p["depends_on"] else "none"
        print(f"  Phase {p['phase']}: {p['name']} ({p['version']}) — {len(p['new_files'])} files, {len(p['new_folders'])} folders | depends on: {deps}")

def main():
    parser = argparse.ArgumentParser(description=INSTALLER_TITLE)
    parser.add_argument("--install", action="store_true", help="Actually install; default is dry run")
    parser.add_argument("--phase", type=int, choices=[p["phase"] for p in PHASES], help="Install only one phase")
    parser.add_argument("--target", type=str, default=DEFAULT_TARGET, help=f"Project root; default: {DEFAULT_TARGET}")
    parser.add_argument("--summary", action="store_true", help="Show summary")
    args = parser.parse_args()

    target = Path(args.target).resolve()
    dry_run = not args.install

    print("\n" + "═"*64)
    print(f"  {INSTALLER_TITLE}")
    print(f"  Target: {target}")
    print(f"  Mode: {'INSTALL' if args.install else 'DRY RUN'}")
    print("═"*64)

    if args.summary:
        show_summary()
        return

    if not preflight_check(target) and args.install:
        print("\n  ✗ Aborting install due to failed preflight checks.")
        sys.exit(1)

    phases = [p for p in PHASES if not args.phase or p["phase"] == args.phase]
    results = [install_phase(p, target, dry_run) for p in phases]
    write_audit_log(target, results, dry_run)

    print("\n" + "═"*64)
    print(f"  {'INSTALLATION COMPLETE' if args.install else 'DRY RUN COMPLETE'}")
    print(f"  {len(results)} phases | {sum(len(r['folders_created']) for r in results)} folders | {sum(len(r['files_created']) for r in results)} files | {sum(len(r['files_skipped']) for r in results)} skipped | {sum(len(r['errors']) for r in results)} errors")
    if dry_run:
        print("  To install for real, run with --install")
    print("═"*64 + "\n")

if __name__ == "__main__":
    main()
