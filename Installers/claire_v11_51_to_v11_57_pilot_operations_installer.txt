#!/usr/bin/env python3
from __future__ import annotations

import argparse
import json
import shutil
from datetime import datetime, timezone
from pathlib import Path
from typing import Any, Dict, List

INSTALLER_NAME = 'Claire v11.51-v11.57 Pilot Operations Installer'
INSTALLER_VERSION = '11.51-11.57'
DEFAULT_TARGET = Path.home() / "OneDrive" / "Desktop" / "Claire"

FOLDERS = [
  "src/claire/pilot_operations",
  "tests/pilot_operations",
  "data/pilot_operations/pilot_runs",
  "data/pilot_operations/operator_sessions",
  "data/pilot_operations/acceptance_criteria",
  "data/pilot_operations/roi_observations",
  "data/pilot_operations/buyer_feedback",
  "data/pilot_operations/adoption_signals",
  "docs/pilot_operations",
  ".claire_install/reports"
]
FILES = {
  "src/claire/pilot_operations/__init__.py": "\"\"\"Claire pilot operations package.\"\"\"\n",
  "src/claire/pilot_operations/pilot_run_registry.py": "from __future__ import annotations\nimport json\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any, Dict\n\nclass PilotRunRegistry:\n    def __init__(self, root: str | Path = \".\") -> None:\n        self.root = Path(root)\n\n    def create_pilot_run(self, pilot_id: str, objective: str, owner: str = \"operator\") -> Dict[str, Any]:\n        return {\n            \"record_type\": \"pilot_run\",\n            \"pilot_id\": pilot_id,\n            \"objective\": objective,\n            \"owner\": owner,\n            \"status\": \"created\",\n            \"created_at_utc\": datetime.now(timezone.utc).isoformat(),\n        }\n\n    def export_run(self, run: Dict[str, Any]) -> Path:\n        out = self.root / \"data\" / \"pilot_operations\" / \"pilot_runs\" / f\"{run['pilot_id']}.json\"\n        out.parent.mkdir(parents=True, exist_ok=True)\n        out.write_text(json.dumps(run, indent=2, sort_keys=True), encoding=\"utf-8\")\n        return out\n",
  "src/claire/pilot_operations/operator_session_logger.py": "from __future__ import annotations\nimport json\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any, Dict\n\nclass OperatorSessionLogger:\n    def __init__(self, root: str | Path = \".\") -> None:\n        self.root = Path(root)\n\n    def log_session(self, session_id: str, action: str, notes: str = \"\") -> Dict[str, Any]:\n        return {\n            \"record_type\": \"operator_session\",\n            \"session_id\": session_id,\n            \"action\": action,\n            \"notes\": notes,\n            \"logged_at_utc\": datetime.now(timezone.utc).isoformat(),\n        }\n\n    def export_session(self, record: Dict[str, Any]) -> Path:\n        out = self.root / \"data\" / \"pilot_operations\" / \"operator_sessions\" / f\"{record['session_id']}_{record['logged_at_utc'].replace(':','-')}.json\"\n        out.parent.mkdir(parents=True, exist_ok=True)\n        out.write_text(json.dumps(record, indent=2, sort_keys=True), encoding=\"utf-8\")\n        return out\n",
  "src/claire/pilot_operations/pilot_acceptance_criteria.py": "from __future__ import annotations\nfrom typing import Any, Dict\n\nclass PilotAcceptanceCriteria:\n    REQUIRED = {\n        \"operator_review_count\": 5,\n        \"benchmark_replay_count\": 5,\n        \"accepted_output_count\": 3,\n        \"critical_false_positive_count\": 0,\n    }\n\n    def evaluate(self, evidence: Dict[str, int]) -> Dict[str, Any]:\n        failures = []\n        for key, required in self.REQUIRED.items():\n            actual = int(evidence.get(key, 0))\n            if key == \"critical_false_positive_count\":\n                passed = actual <= required\n            else:\n                passed = actual >= required\n            if not passed:\n                failures.append({\"metric\": key, \"required\": required, \"actual\": actual})\n        return {\"status\": \"pass\" if not failures else \"blocked\", \"failures\": failures}\n",
  "src/claire/pilot_operations/roi_observation_tracker.py": "from __future__ import annotations\nimport json\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any, Dict\n\nclass ROIObservationTracker:\n    def __init__(self, root: str | Path = \".\") -> None:\n        self.root = Path(root)\n\n    def build_observation(self, pilot_id: str, time_saved_minutes: float = 0, value_note: str = \"\") -> Dict[str, Any]:\n        return {\n            \"record_type\": \"roi_observation\",\n            \"pilot_id\": pilot_id,\n            \"time_saved_minutes\": time_saved_minutes,\n            \"value_note\": value_note,\n            \"observed_at_utc\": datetime.now(timezone.utc).isoformat(),\n        }\n\n    def export_observation(self, obs: Dict[str, Any]) -> Path:\n        out = self.root / \"data\" / \"pilot_operations\" / \"roi_observations\" / f\"{obs['pilot_id']}_{obs['observed_at_utc'].replace(':','-')}.json\"\n        out.parent.mkdir(parents=True, exist_ok=True)\n        out.write_text(json.dumps(obs, indent=2, sort_keys=True), encoding=\"utf-8\")\n        return out\n",
  "src/claire/pilot_operations/buyer_feedback_collector.py": "from __future__ import annotations\nimport json\nfrom datetime import datetime, timezone\nfrom pathlib import Path\nfrom typing import Any, Dict\n\nclass BuyerFeedbackCollector:\n    def __init__(self, root: str | Path = \".\") -> None:\n        self.root = Path(root)\n\n    def collect_feedback(self, pilot_id: str, buyer_segment: str, feedback: str, rating: int = 0) -> Dict[str, Any]:\n        return {\n            \"record_type\": \"buyer_feedback\",\n            \"pilot_id\": pilot_id,\n            \"buyer_segment\": buyer_segment,\n            \"feedback\": feedback,\n            \"rating\": rating,\n            \"collected_at_utc\": datetime.now(timezone.utc).isoformat(),\n        }\n\n    def export_feedback(self, record: Dict[str, Any]) -> Path:\n        out = self.root / \"data\" / \"pilot_operations\" / \"buyer_feedback\" / f\"{record['pilot_id']}_{record['collected_at_utc'].replace(':','-')}.json\"\n        out.parent.mkdir(parents=True, exist_ok=True)\n        out.write_text(json.dumps(record, indent=2, sort_keys=True), encoding=\"utf-8\")\n        return out\n",
  "docs/pilot_operations/v11_51_to_v11_57_pilot_operations_plan.md": "# v11.51-v11.57 Pilot Operations\n\nControlled pilot operations evidence only. Do not expand autonomy.\n",
  "tests/pilot_operations/test_pilot_operations.py": "from claire.pilot_operations.pilot_acceptance_criteria import PilotAcceptanceCriteria\nfrom claire.pilot_operations.pilot_run_registry import PilotRunRegistry\n\ndef test_pilot_run_registry():\n    run = PilotRunRegistry().create_pilot_run(\"p1\", \"test pilot\")\n    assert run[\"status\"] == \"created\"\n\ndef test_pilot_acceptance_blocks_empty():\n    result = PilotAcceptanceCriteria().evaluate({})\n    assert result[\"status\"] == \"blocked\"\n"
}
VERSION_FILE_NAME = 'version_11_57_pilot_operations.json'
LOCK_PAYLOAD = {
  "version": "11.57",
  "state": "pilot_operations_installed",
  "phase": "controlled_pilot_operations",
  "status": "installed_not_proven",
  "safety_contract": {
    "does_not_modify_runtime": True,
    "does_not_modify_routing": True,
    "does_not_modify_scoring": True,
    "pilot_evidence_only": True
  }
}

def stamp() -> str:
    return datetime.now(timezone.utc).strftime("%Y%m%d_%H%M%S")

def now_iso() -> str:
    return datetime.now(timezone.utc).isoformat()

def ensure_dir(path: Path, install: bool, actions: List[Dict[str, Any]]) -> None:
    existed = path.exists()
    if install:
        path.mkdir(parents=True, exist_ok=True)
    actions.append({"action": "ensure_dir", "path": str(path), "installed": install, "existed": existed})

def write_file(path: Path, content: str, install: bool, actions: List[Dict[str, Any]]) -> None:
    existed = path.exists()
    backup = None
    if install:
        path.parent.mkdir(parents=True, exist_ok=True)
        if existed:
            backup_path = path.with_name(path.name + ".bak_" + stamp())
            shutil.copy2(path, backup_path)
            backup = str(backup_path)
        path.write_text(content, encoding="utf-8", newline="\n")
    actions.append({"action": "write_file", "path": str(path), "installed": install, "existed": existed, "backup": backup})

def main() -> int:
    parser = argparse.ArgumentParser(description=INSTALLER_NAME)
    parser.add_argument("--target", default=str(DEFAULT_TARGET), help="Claire project root")
    parser.add_argument("--install", action="store_true", help="Apply install. Without this, dry run only.")
    args = parser.parse_args()

    root = Path(args.target).expanduser().resolve()
    install = bool(args.install)

    print(f"{INSTALLER_NAME} {INSTALLER_VERSION}")
    print(f"Target: {root}")
    print("Mode:", "INSTALL" if install else "DRY RUN")

    if not root.exists():
        print(f"[ERROR] Target does not exist: {root}")
        return 2
    if not (root / "src").exists():
        print("[ERROR] This does not look like the Claire project root. Missing src folder.")
        return 3

    actions: List[Dict[str, Any]] = []
    for folder in FOLDERS:
        ensure_dir(root / folder, install, actions)
    for rel, content in FILES.items():
        write_file(root / rel, content, install, actions)

    payload = dict(LOCK_PAYLOAD)
    payload["installed_at_utc"] = now_iso()
    write_file(root / VERSION_FILE_NAME, json.dumps(payload, indent=2, sort_keys=True), install, actions)

    report = {
        "installer": INSTALLER_NAME,
        "installer_version": INSTALLER_VERSION,
        "installed": install,
        "installed_at_utc": now_iso(),
        "target": str(root),
        "actions": actions,
        "version_file": VERSION_FILE_NAME,
        "safety_contract": payload.get("safety_contract", {}),
        "post_install_checks": [
            "Run the targeted pytest folder for this installer.",
            "Confirm new src/claire package folder exists.",
            "Confirm new data folders exist.",
            "Commit after tests pass."
        ]
    }

    if install:
        report_path = root / ".claire_install" / "reports" / (VERSION_FILE_NAME.replace(".json", "") + "_" + stamp() + ".json")
        report_path.parent.mkdir(parents=True, exist_ok=True)
        report_path.write_text(json.dumps(report, indent=2, sort_keys=True), encoding="utf-8")
        print(f"[OK] Report written: {report_path}")
    else:
        print("[DRY RUN] No files changed. Re-run with --install to apply.")

    print(json.dumps({
        "installed": install,
        "target": str(root),
        "installer_version": INSTALLER_VERSION,
        "folders": len(FOLDERS),
        "files": len(FILES) + 1
    }, indent=2))
    return 0

if __name__ == "__main__":
    raise SystemExit(main())

