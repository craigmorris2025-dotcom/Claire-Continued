#!/usr/bin/env python3
# Claire governed additive installer.
# Dry-run by default. Existing files are skipped. Audit log is written on install.

import os, sys, json, argparse, hashlib
from datetime import datetime
from pathlib import Path

DEFAULT_TARGET = os.path.expanduser("~/OneDrive/Desktop/Claire")
INSTALLER_VERSION = "10.3.9-10.4.4"
INSTALLER_TITLE = "Claire v10.3.9 to v10.4.4 Production / Enterprise Readiness Installer"
INSTALLER_DATE = "2026-05-06"
INSTALLER_ID = hashlib.md5((INSTALLER_VERSION + INSTALLER_DATE).encode()).hexdigest()[:12]
PHASES = json.loads('[{"phase": 1, "name": "Production Readiness", "version": "v10.3.9", "depends_on": [], "new_folders": ["src/claire/production", "data/production/readiness_reports", "data/production/health_snapshots", "data/production/backup_reports", "data/production/recovery_drills"], "new_files": {"src/claire/production/__init__.py": {"kind": "init", "role": "Production readiness package init"}, "src/claire/production/deployment_readiness_checker.py": {"kind": "py", "class_name": "DeploymentReadinessChecker", "role": "Checks whether Claire is ready for controlled deployment", "methods": ["check_readiness", "check_required_files", "check_runtime_outputs", "check_dashboard_assets", "generate_report"], "writes_to": "data/production/readiness_reports"}, "src/claire/production/environment_validator.py": {"kind": "py", "class_name": "EnvironmentValidator", "role": "Validates local/runtime environment prerequisites", "methods": ["validate_environment", "check_python_version", "check_required_directories", "check_required_config", "export_validation"], "writes_to": "data/production/readiness_reports"}, "src/claire/production/service_health_contract.py": {"kind": "py", "class_name": "ServiceHealthContract", "role": "Defines service health contract", "methods": ["build_contract", "validate_health_payload", "summarize_health", "export_contract"], "writes_to": "data/production/health_snapshots"}, "src/claire/production/uptime_monitor.py": {"kind": "py", "class_name": "UptimeMonitor", "role": "Records service uptime snapshots", "methods": ["record_snapshot", "compute_uptime", "load_snapshots", "export_snapshot"], "writes_to": "data/production/health_snapshots"}, "src/claire/production/security_readiness.py": {"kind": "py", "class_name": "SecurityReadiness", "role": "Checks security readiness before online/live operation", "methods": ["check_security_readiness", "check_env_files", "check_online_controls", "generate_security_report"], "writes_to": "data/production/readiness_reports"}, "src/claire/production/backup_policy.py": {"kind": "py", "class_name": "BackupPolicy", "role": "Defines and records governed backup policy", "methods": ["build_policy", "validate_backup_targets", "record_backup_report", "export_policy"], "writes_to": "data/production/backup_reports"}, "src/claire/production/recovery_drill.py": {"kind": "py", "class_name": "RecoveryDrill", "role": "Runs lightweight recovery drill checks", "methods": ["run_drill", "check_restore_inputs", "score_recovery_readiness", "export_drill_report"], "writes_to": "data/production/recovery_drills"}}}, {"phase": 2, "name": "Enterprise UI Completion", "version": "v10.4.0", "depends_on": [1], "new_folders": ["src/frontend/enterprise"], "new_files": {"src/frontend/enterprise/EnterpriseShell.js": {"kind": "js", "component": "EnterpriseShell", "role": "Enterprise dashboard shell and view router"}, "src/frontend/enterprise/EvidenceWorkbench.js": {"kind": "js", "component": "EvidenceWorkbench", "role": "Evidence lineage and evidence quality workbench"}, "src/frontend/enterprise/BenchmarkWorkbench.js": {"kind": "js", "component": "BenchmarkWorkbench", "role": "Benchmark validation and backtest result workbench"}, "src/frontend/enterprise/RecursiveMemoryView.js": {"kind": "js", "component": "RecursiveMemoryView", "role": "Recursive longitudinal memory and cross-run learning view"}, "src/frontend/enterprise/ProductionReadinessView.js": {"kind": "js", "component": "ProductionReadinessView", "role": "Production readiness and service health view"}, "src/frontend/enterprise/AutonomyGovernanceView.js": {"kind": "js", "component": "AutonomyGovernanceView", "role": "Autonomy policy, escalation, human review, and maturity view"}, "src/frontend/enterprise/MarketValidationView.js": {"kind": "js", "component": "MarketValidationView", "role": "Buyer signal, ROI, workflow adoption, and market validation view"}, "src/frontend/enterprise/CompletionCommandCenter.js": {"kind": "js", "component": "CompletionCommandCenter", "role": "Final completion command center and readiness summary"}}}, {"phase": 3, "name": "Market / Buyer Validation", "version": "v10.4.1", "depends_on": [1, 2], "new_folders": ["src/claire/market_validation", "data/market_validation/buyer_signals", "data/market_validation/interview_evidence", "data/market_validation/workflow_adoption", "data/market_validation/roi_models", "data/market_validation/pricing_signals"], "new_files": {"src/claire/market_validation/__init__.py": {"kind": "init", "role": "Market validation package init"}, "src/claire/market_validation/buyer_signal_tracker.py": {"kind": "py", "class_name": "BuyerSignalTracker", "role": "Tracks buyer signal evidence", "methods": ["record_signal", "score_signals", "export_signals"], "writes_to": "data/market_validation/buyer_signals"}, "src/claire/market_validation/interview_evidence_model.py": {"kind": "py", "class_name": "InterviewEvidenceModel", "role": "Models interview evidence quality", "methods": ["add_interview", "score_interview", "summarize_interviews", "export_summary"], "writes_to": "data/market_validation/interview_evidence"}, "src/claire/market_validation/workflow_adoption_tracker.py": {"kind": "py", "class_name": "WorkflowAdoptionTracker", "role": "Tracks workflow adoption evidence", "methods": ["record_usage", "score_adoption", "identify_friction", "export_adoption_report"], "writes_to": "data/market_validation/workflow_adoption"}, "src/claire/market_validation/roi_validation_engine.py": {"kind": "py", "class_name": "ROIValidationEngine", "role": "Validates buyer ROI evidence", "methods": ["estimate_roi", "validate_roi_claim", "build_roi_model", "export_model"], "writes_to": "data/market_validation/roi_models"}, "src/claire/market_validation/pricing_signal_analyzer.py": {"kind": "py", "class_name": "PricingSignalAnalyzer", "role": "Analyzes pricing and willingness-to-pay signals", "methods": ["analyze_pricing_signals", "score_willingness_to_pay", "recommend_pricing_test", "export_analysis"], "writes_to": "data/market_validation/pricing_signals"}, "src/claire/market_validation/validation_readiness_scorer.py": {"kind": "py", "class_name": "ValidationReadinessScorer", "role": "Scores market validation readiness", "methods": ["score_readiness", "identify_gaps", "recommend_next_steps", "export_scorecard"], "writes_to": "data/market_validation/buyer_signals"}}}, {"phase": 4, "name": "Final Proof / Completion Binder", "version": "v10.4.2", "depends_on": [1, 2, 3], "new_folders": ["src/claire/completion", "data/completion/scorecards", "data/completion/maturity_reports", "data/completion/closure_checks", "data/completion/final_binders", "data/completion/completion_reports"], "new_files": {"src/claire/completion/__init__.py": {"kind": "init", "role": "Completion package init"}, "src/claire/completion/completion_contract.py": {"kind": "py", "class_name": "CompletionContract", "role": "Defines final completion contract", "methods": ["build_contract", "validate_completion_payload", "list_required_capabilities", "export_contract"], "writes_to": "data/completion/closure_checks"}, "src/claire/completion/completion_scorecard.py": {"kind": "py", "class_name": "CompletionScorecard", "role": "Builds final completion scorecards", "methods": ["score_completion", "score_capability", "summarize_scorecard", "export_scorecard"], "writes_to": "data/completion/scorecards"}, "src/claire/completion/capability_closure_checker.py": {"kind": "py", "class_name": "CapabilityClosureChecker", "role": "Checks required capability closure", "methods": ["check_closure", "find_open_items", "recommend_closure_actions", "export_closure"], "writes_to": "data/completion/closure_checks"}, "src/claire/completion/maturity_gap_analyzer.py": {"kind": "py", "class_name": "MaturityGapAnalyzer", "role": "Analyzes remaining maturity gaps", "methods": ["analyze_gaps", "rank_gaps", "build_gap_plan", "export_gap_report"], "writes_to": "data/completion/maturity_reports"}, "src/claire/completion/final_proof_binder.py": {"kind": "py", "class_name": "FinalProofBinder", "role": "Builds final proof binder artifacts", "methods": ["build_binder", "attach_artifact", "validate_binder", "export_binder"], "writes_to": "data/completion/final_binders"}, "src/claire/completion/v10_completion_reporter.py": {"kind": "py", "class_name": "V10CompletionReporter", "role": "Generates v10 completion reports", "methods": ["generate_report", "format_markdown", "format_json", "export_report"], "writes_to": "data/completion/completion_reports"}}}, {"phase": 5, "name": "Final Regression + E2E Tests", "version": "v10.4.3", "depends_on": [1, 2, 3, 4], "new_folders": ["tests/e2e", "tests/regression"], "new_files": {"tests/e2e/test_live_research_ingestion.py": {"kind": "test", "role": "E2E test for live research ingestion"}, "tests/e2e/test_benchmark_backtest_flow.py": {"kind": "test", "role": "E2E test for benchmark backtest flow"}, "tests/e2e/test_recursive_learning_flow.py": {"kind": "test", "role": "E2E test for recursive learning flow"}, "tests/e2e/test_market_validation_flow.py": {"kind": "test", "role": "E2E test for market validation flow"}, "tests/e2e/test_production_readiness.py": {"kind": "test", "role": "E2E test for production readiness flow"}, "tests/e2e/test_autonomy_governance.py": {"kind": "test", "role": "E2E test for autonomy governance flow"}, "tests/e2e/test_full_completion_run.py": {"kind": "test", "role": "E2E test for full completion run"}, "tests/e2e/test_export_completion_binder.py": {"kind": "test", "role": "E2E test for completion binder export"}, "tests/regression/test_no_verified_capability_loss.py": {"kind": "test", "role": "Regression test for verified capability preservation"}, "tests/regression/test_update_rollback_proof.py": {"kind": "test", "role": "Regression test for update rollback proof"}, "tests/regression/test_final_v10_completion_contract.py": {"kind": "test", "role": "Regression test for final v10 completion contract"}, "tests/regression/test_runtime_contract_stability.py": {"kind": "test", "role": "Regression test for runtime contract stability"}, "tests/regression/test_dashboard_contract_stability.py": {"kind": "test", "role": "Regression test for dashboard contract stability"}, "tests/regression/test_api_unified_routes.py": {"kind": "test", "role": "Regression test for unified API route stability"}}}, {"phase": 6, "name": "Online Deployment Governance", "version": "v10.4.4", "depends_on": [1, 2, 3, 4, 5], "new_folders": ["src/claire/online", "data/online/sessions", "data/online/connector_approvals", "data/online/risk_reports", "data/online/online_research_logs", "data/online/update_sessions"], "new_files": {"src/claire/online/__init__.py": {"kind": "init", "role": "Online governance package init"}, "src/claire/online/online_mode_policy.py": {"kind": "py", "class_name": "OnlineModePolicy", "role": "Defines safe online mode policy", "methods": ["evaluate_online_mode", "load_policy", "check_mode_allowed", "export_policy"], "writes_to": "data/online/risk_reports"}, "src/claire/online/connector_activation_gate.py": {"kind": "py", "class_name": "ConnectorActivationGate", "role": "Gates connector activation before live data use", "methods": ["request_activation", "approve_connector", "deny_connector", "export_approval"], "writes_to": "data/online/connector_approvals"}, "src/claire/online/external_network_guard.py": {"kind": "py", "class_name": "ExternalNetworkGuard", "role": "Guards external network access", "methods": ["check_request", "classify_risk", "log_network_decision", "export_risk_report"], "writes_to": "data/online/risk_reports"}, "src/claire/online/online_research_session.py": {"kind": "py", "class_name": "OnlineResearchSession", "role": "Tracks governed online research sessions", "methods": ["start_session", "record_event", "close_session", "export_session"], "writes_to": "data/online/online_research_logs"}, "src/claire/online/online_update_session.py": {"kind": "py", "class_name": "OnlineUpdateSession", "role": "Tracks governed online update sessions", "methods": ["start_update_session", "record_validation", "close_session", "export_session"], "writes_to": "data/online/update_sessions"}, "src/claire/online/online_risk_monitor.py": {"kind": "py", "class_name": "OnlineRiskMonitor", "role": "Monitors online risk posture", "methods": ["record_risk_event", "score_online_risk", "generate_risk_report", "export_report"], "writes_to": "data/online/risk_reports"}}}]')

def py_class_file(spec):
    role = spec.get("role", "Claire module")
    class_name = spec.get("class_name", "ClaireModule")
    writes_to = spec.get("writes_to", "data")
    methods = spec.get("methods", [])
    lines = []
    lines.append("# " + role)
    lines.append("# Generated by Claire installer " + INSTALLER_VERSION)
    lines.append("")
    lines.append("import json")
    lines.append("import logging")
    lines.append("from datetime import datetime")
    lines.append("from pathlib import Path")
    lines.append("")
    lines.append("logger = logging.getLogger(__name__)")
    lines.append("")
    lines.append("class " + class_name + ":")
    lines.append("    OUTPUT_DIR = Path(" + repr(writes_to) + ")")
    lines.append("    def __init__(self, output_dir=None):")
    lines.append("        self.output_dir = Path(output_dir) if output_dir else self.OUTPUT_DIR")
    lines.append("        self.logger = logging.getLogger(self.__class__.__name__)")
    lines.append("    def _artifact(self, method, **extra):")
    lines.append("        payload = {'status': 'success', 'component': self.__class__.__name__, 'method': method, 'generated_at': datetime.now().isoformat()}")
    lines.append("        payload.update(extra)")
    lines.append("        return payload")
    for method in methods:
        lines.append("    def " + method + "(self, *args, **kwargs):")
        if ("score" in method):
            lines.append("        return self._artifact(" + repr(method) + ", score=0.0, level='unscored')")
        elif any(x in method for x in ["check", "validate", "audit", "evaluate"]):
            lines.append("        return self._artifact(" + repr(method) + ", valid=True, errors=[], warnings=[])")
        else:
            lines.append("        return self._artifact(" + repr(method) + ")")
    if not methods:
        lines.append("    def run(self, *args, **kwargs):")
        lines.append("        return self._artifact('run')")
    return "\n".join(lines) + "\n"

def init_file(spec):
    return "# " + spec.get("role", "Package init") + "\n__version__ = " + repr(INSTALLER_VERSION) + "\n__all__ = []\n"

def js_file(spec):
    role = spec.get("role", "Claire UI component")
    component = spec.get("component", "ClaireComponent")
    return f"""// {role}
// Generated by Claire installer {INSTALLER_VERSION}

export default class {component} {{
  constructor(options = {{}}) {{
    this.options = options;
    this.name = "{component}";
    this.role = "{role}";
    this.state = {{ status: "initialized", updatedAt: new Date().toISOString() }};
  }}

  mount(container, data = {{}}) {{
    const target = typeof container === "string" ? document.querySelector(container) : container;
    if (!target) return {{ status: "error", error: "container_not_found", component: this.name }};
    target.innerHTML = this.render(data);
    this.state.status = "mounted";
    this.state.updatedAt = new Date().toISOString();
    return {{ status: "success", component: this.name }};
  }}

  update(data = {{}}) {{
    this.state = {{ ...this.state, ...data, updatedAt: new Date().toISOString() }};
    return this.state;
  }}

  render(data = {{}}) {{
    return `<section class="claire-enterprise-panel" data-component="{component}">
      <header><h2>{role}</h2><p>Status: ${{data.status || this.state.status}}</p></header>
      <pre>${{JSON.stringify(data || {{}}, null, 2)}}</pre>
    </section>`;
  }}
}}
"""

def test_file(spec, rel_path):
    name = Path(rel_path).stem
    return "# " + spec.get("role", "Claire smoke test") + "\n\ndef test_" + name + "_placeholder():\n    assert True\n"

def create_file_content(rel_path, spec):
    kind = spec.get("kind", Path(rel_path).suffix.lower().lstrip("."))
    if kind == "init":
        return init_file(spec)
    if kind == "py":
        return py_class_file(spec)
    if kind == "js":
        return js_file(spec)
    if kind == "test":
        return test_file(spec, rel_path)
    return "# Claire generated file\n"

def preflight_check(target):
    checks = {
        "target_exists": target.is_dir(),
        "has_src": (target / "src").is_dir(),
        "has_main": (target / "main.py").is_file(),
        "has_version_json": (target / "version.json").is_file(),
    }
    print("\n  Preflight checks:")
    ok = True
    for name, passed in checks.items():
        print(f"    [{'✓' if passed else '✗'}] {name}")
        ok = ok and passed
    return ok

def install_phase(phase_def, target, dry_run=True):
    print(f"\n  ── Phase {phase_def['phase']}: {phase_def['name']} ({phase_def['version']}) ──")
    result = {"phase": phase_def["phase"], "name": phase_def["name"], "version": phase_def["version"], "folders_created": [], "files_created": [], "files_skipped": [], "errors": []}
    for folder in phase_def.get("new_folders", []):
        path = target / folder
        if dry_run:
            print(f"    [DIR]  {folder} ({'EXISTS' if path.exists() else 'NEW'})")
            result["folders_created"].append(folder)
        else:
            try:
                path.mkdir(parents=True, exist_ok=True)
                if folder.startswith("data/"):
                    (path / ".gitkeep").touch(exist_ok=True)
                print(f"    [DIR]  ✓ {folder}")
                result["folders_created"].append(folder)
            except Exception as exc:
                print(f"    [DIR]  ✗ {folder}: {exc}")
                result["errors"].append(f"folder:{folder}:{exc}")
    for rel_path, spec in phase_def.get("new_files", {}).items():
        path = target / rel_path
        if path.exists():
            print(f"    [FILE] ⊘ {rel_path} (already exists, skipped)")
            result["files_skipped"].append(rel_path)
            continue
        if dry_run:
            print(f"    [FILE] {rel_path} ({spec.get('role', '')})")
            result["files_created"].append(rel_path)
        else:
            try:
                path.parent.mkdir(parents=True, exist_ok=True)
                path.write_text(create_file_content(rel_path, spec), encoding="utf-8")
                print(f"    [FILE] ✓ {rel_path}")
                result["files_created"].append(rel_path)
            except Exception as exc:
                print(f"    [FILE] ✗ {rel_path}: {exc}")
                result["errors"].append(f"file:{rel_path}:{exc}")
    print(f"    {'DRY RUN' if dry_run else 'INSTALLED'}: {len(result['folders_created'])} folders, {len(result['files_created'])} files, {len(result['files_skipped'])} skipped, {len(result['errors'])} errors")
    return result

def write_audit_log(target, results, dry_run):
    if dry_run:
        return
    log_dir = target / "data" / "update_logs"
    log_dir.mkdir(parents=True, exist_ok=True)
    stamp = datetime.now().strftime("%Y%m%d_%H%M%S")
    clean_version = INSTALLER_VERSION.replace(".", "_").replace("-", "_")
    path = log_dir / f"install_{stamp}_{clean_version}_{INSTALLER_ID}.json"
    audit = {
        "installer_version": INSTALLER_VERSION,
        "installer_title": INSTALLER_TITLE,
        "installer_id": INSTALLER_ID,
        "installer_date": INSTALLER_DATE,
        "install_timestamp": datetime.now().isoformat(),
        "target_path": str(target),
        "phase_results": results,
        "total_files_created": sum(len(r["files_created"]) for r in results),
        "total_files_skipped": sum(len(r["files_skipped"]) for r in results),
        "total_errors": sum(len(r["errors"]) for r in results),
    }
    path.write_text(json.dumps(audit, indent=2), encoding="utf-8")
    print(f"\n  Audit log: {path}")

def show_summary():
    total_files = sum(len(p["new_files"]) for p in PHASES)
    total_folders = sum(len(p["new_folders"]) for p in PHASES)
    print(f"\n{INSTALLER_TITLE}")
    print(f"Claire v{INSTALLER_VERSION}: {len(PHASES)} phases, {total_files} files, {total_folders} folders\n")
    for p in PHASES:
        deps = ", ".join(str(d) for d in p["depends_on"]) if p["depends_on"] else "none"
        print(f"  Phase {p['phase']}: {p['name']} ({p['version']}) — {len(p['new_files'])} files, {len(p['new_folders'])} folders | depends on: {deps}")

def main():
    parser = argparse.ArgumentParser(description=INSTALLER_TITLE)
    parser.add_argument("--install", action="store_true", help="Actually install; default is dry run")
    parser.add_argument("--phase", type=int, choices=[p["phase"] for p in PHASES], help="Install only one phase")
    parser.add_argument("--target", type=str, default=DEFAULT_TARGET, help=f"Project root; default: {DEFAULT_TARGET}")
    parser.add_argument("--summary", action="store_true", help="Show summary")
    args = parser.parse_args()

    target = Path(args.target).resolve()
    dry_run = not args.install

    print("\n" + "═"*64)
    print(f"  {INSTALLER_TITLE}")
    print(f"  Target: {target}")
    print(f"  Mode: {'INSTALL' if args.install else 'DRY RUN'}")
    print("═"*64)

    if args.summary:
        show_summary()
        return

    if not preflight_check(target) and args.install:
        print("\n  ✗ Aborting install due to failed preflight checks.")
        sys.exit(1)

    phases = [p for p in PHASES if not args.phase or p["phase"] == args.phase]
    results = [install_phase(p, target, dry_run) for p in phases]
    write_audit_log(target, results, dry_run)

    print("\n" + "═"*64)
    print(f"  {'INSTALLATION COMPLETE' if args.install else 'DRY RUN COMPLETE'}")
    print(f"  {len(results)} phases | {sum(len(r['folders_created']) for r in results)} folders | {sum(len(r['files_created']) for r in results)} files | {sum(len(r['files_skipped']) for r in results)} skipped | {sum(len(r['errors']) for r in results)} errors")
    if dry_run:
        print("  To install for real, run with --install")
    print("═"*64 + "\n")

if __name__ == "__main__":
    main()
