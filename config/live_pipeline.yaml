# Live pipeline configuration and connector settings
# Version: 6.0.0
# Architecture: LOCKED at v5.90.2
# Module: live

version: "6.0.0"
architecture_status: "LOCKED"
base_version: "5.90.2"

module:
  name: "live"
  enabled: true

# TODO: Add module-specific configuration
settings:
  log_level: "INFO"
  confidence_threshold: 0.70
  max_retries: 3
  timeout_seconds: 300
