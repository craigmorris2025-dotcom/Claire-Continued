# Claire S45 Cockpit Bridge

This folder contains presentation-only cockpit bridge assets for S45R1-R8.

The bridge may fetch read-only backend-owned operator surfaces by `GET`.
It must not mutate runtime truth, trigger automatic updates, run autonomous
execution, or perform browser automation.
