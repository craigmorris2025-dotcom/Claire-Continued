{
  "rules": [
    {
      "name": "strategy_synergy_fusion",
      "inputs": ["strategy_score", "synergy_score"],
      "method": "weighted_average",
      "weights": [0.6, 0.4],
      "output": "strategic_alignment",
      "description": "Fuses strategy and synergy into strategic alignment composite"
    },
    {
      "name": "market_innovation_fusion",
      "inputs": ["market_score", "innovation_score"],
      "method": "weighted_average",
      "weights": [0.5, 0.5],
      "output": "market_innovation_potential",
      "description": "Fuses market and innovation into combined potential score"
    },
    {
      "name": "deal_risk_fusion",
      "inputs": ["deal_score", "risk_score"],
      "method": "risk_adjusted",
      "weights": [0.7, 0.3],
      "output": "deal_viability",
      "description": "Risk-adjusts deal score for viability assessment"
    },
    {
      "name": "breakthrough_confidence_fusion",
      "inputs": ["breakthrough_score", "_confidence"],
      "method": "geometric_mean",
      "weights": [1.0, 1.0],
      "output": "breakthrough_confidence",
      "description": "Geometric mean of breakthrough and confidence for calibrated assessment"
    },
    {
      "name": "portfolio_composite_fusion",
      "inputs": ["portfolio_score", "forecast_score", "compliance_score"],
      "method": "weighted_average",
      "weights": [0.5, 0.3, 0.2],
      "output": "portfolio_readiness",
      "description": "Composite portfolio readiness from multiple dimensions"
    },
    {
      "name": "acquirer_fit_fusion",
      "inputs": ["acquirer_score", "strategy_score", "tech_alignment_score"],
      "method": "weighted_average",
      "weights": [0.4, 0.35, 0.25],
      "output": "acquirer_compatibility",
      "description": "Overall acquirer compatibility from fit and alignment scores"
    }
  ],
  "methods": {
    "weighted_average": "Sum of (score * weight) / sum of weights",
    "risk_adjusted": "Primary score * (1 - risk_weight * risk_score)",
    "geometric_mean": "Nth root of product of scores"
  }
}
