# Claire v17.62.5 — Dashboard Conflict Cleanup + Stable Runtime Stack

## Purpose

The screenshots after v17.62.4 show that multiple dashboard-injection layers are fighting each other:

- old floating search overlay returned
- validation bridge panel overlaps command
- memory bridge panel overlaps validation
- proof/status rail cuts across the workspace
- command cards are clipped by older internal scroll containers

v17.62.5 cleans this up by removing old conflicting dashboard injections and rendering a single stable runtime-truth stack.

## What changed

Installed:

```text
src/frontend/command_center/modern/claire_dashboard_reset_v17_62_5.css
src/frontend/command_center/modern/claire_dashboard_reset_v17_62_5.js
```

Patched:

```text
src/frontend/command_center/modern/index.html
```

The installer also removes old conflicting v17.62-series link/script tags from the dashboard HTML before injecting the clean reset layer.

## Dashboard behavior

- Search is embedded in the Claire identity/sidebar area.
- Old floating search shells are removed.
- Old generated validation/memory panels are hidden.
- New clean runtime stack shows run, route, terminal, stages, validation, evidence, memory, and recursion.
- Runtime truth remains honest; missing data is shown as not_loaded, missing, blocked, or not_generated.

## Scope

No runtime logic changed.
No validation logic changed.
No memory logic changed.
This is a dashboard conflict cleanup layer.
