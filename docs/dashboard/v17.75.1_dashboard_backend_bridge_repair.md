# Claire v17.75.1 - Dashboard Backend Bridge Repair

This build repairs the visible issue where the dashboard opens but shows:

- Backend Offline
- No runtime truth loaded
- Route not loaded
- Discovery / AutoDesign / Design Portal missing even when JSON contracts exist

## What changed

- Replaces the operator dashboard state builder with a v17.75.1 source-of-truth bridge.
- Replaces dashboard frontend with a connected cockpit that fetches `/operator/dashboard/state`.
- Replaces `LAUNCH_CLAIRE.bat` so it starts the FastAPI backend and opens the dashboard.
- Keeps missing data visible instead of faking success.

## API routes

- `GET /operator/dashboard/state`
- `GET /dashboard/state`
- `GET /api/dashboard/state`
- `GET /operator/dashboard/summary`
- `POST /operator/dashboard/refresh`

## Critical rule

The dashboard must not pretend that missing surfaces are complete. It must connect to backend truth and show missing data honestly.
