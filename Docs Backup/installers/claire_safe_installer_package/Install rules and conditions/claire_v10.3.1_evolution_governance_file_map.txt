================================================================================
  CLAIRE v10.3.1 — EVOLUTION & UPDATE GOVERNANCE FILE MAP
  ACS2-Claire / Syntalion
  Generated: 2026-05-06
  Installer ID: See installer script header
  Total New Files: 34  |  Total New Folders: 21
================================================================================

FILE MAP LEGEND
───────────────
  [NEW-DIR]   New directory created by this installer
  [NEW-FILE]  New file created by this installer
  [EXISTING]  Pre-existing directory (not created, used as parent)

  Authority Levels:
    OWNER-ONLY     Only Craig can modify
    GOVERNED       Requires full update governance pipeline
    SYSTEM         Claire can modify under governance rules
    APPEND-ONLY    Can add records; cannot modify or delete existing

================================================================================
PHASE 1 — EVOLUTION LAYER (11 files, 1 folder)
================================================================================

Purpose: Claire's self-awareness — who she IS, where she's been, where she's
         going, what's verified, what's protected, and how she learns from her
         own evolution.

Depends On: Nothing (foundational layer)

[EXISTING]  src/claire/
[NEW-DIR]   src/claire/evolution/

  [NEW-FILE] src/claire/evolution/__init__.py
  ├── Role:       Evolution layer package init
  ├── Authority:  GOVERNED
  ├── Exports:    ProjectIdentity, MasterDocumentRegistry, VersionTimeline,
  │               TargetState, CapabilityRegistry, VerifiedStateStore,
  │               RegressionGuard, ForwardMotionPolicy, EvolutionMemory,
  │               EvolutionOrchestrator
  └── Depends:    All evolution modules below

  [NEW-FILE] src/claire/evolution/project_identity.py
  ├── Role:       Claire's canonical self-definition — who she IS
  ├── Authority:  OWNER-ONLY
  ├── Class:      ProjectIdentity
  ├── Fields:     system_name, codename, owner, mission_statement,
  │               core_principles (7), identity_hash
  ├── Methods:    validate_identity(), get_identity_summary(),
  │               identity_drift_check()
  ├── Data I/O:   Reads → data/evolution/identity/
  └── Constraint: Immutable once loaded — no runtime modification

  [NEW-FILE] src/claire/evolution/master_document_registry.py
  ├── Role:       Registry of all governing documents
  ├── Authority:  OWNER-ONLY
  ├── Class:      MasterDocumentRegistry
  ├── Per-Doc:    document_id, title, version, hash, authority_level
  │               (constitutional/governing/operational/reference),
  │               last_verified_date
  ├── Methods:    register_document(), verify_document_integrity(),
  │               get_authority_chain(), detect_conflicts()
  └── Data I/O:   Reads/Writes → data/evolution/origin/

  [NEW-FILE] src/claire/evolution/version_timeline.py
  ├── Role:       Complete version history with proof of completion
  ├── Authority:  APPEND-ONLY
  ├── Class:      VersionTimeline
  ├── Per-Version: version, codename, date_completed, stages_implemented,
  │               modules_added, files_added, test_results, validation_scores,
  │               proof_artifacts
  ├── Methods:    get_timeline(), get_version_detail(v), verify_version_proof(v),
  │               get_current_version(), get_next_target()
  └── Data I/O:   Reads/Writes → data/evolution/timeline/

  [NEW-FILE] src/claire/evolution/target_state.py
  ├── Role:       Defines where Claire is GOING — target completion state
  ├── Authority:  OWNER-ONLY
  ├── Class:      TargetState
  ├── Fields:     target_version, required_capabilities, required_stages (1-30),
  │               required_routes, required_loops (8 ACS2),
  │               required_memory_functions (5), completion_criteria
  ├── Methods:    get_target(), get_gap_to_target(), is_target_reached(),
  │               get_completion_percentage()
  └── Data I/O:   Reads → data/evolution/target_state/

  [NEW-FILE] src/claire/evolution/capability_registry.py
  ├── Role:       Registry of every verified capability Claire has
  ├── Authority:  SYSTEM (governed writes only)
  ├── Class:      CapabilityRegistry
  ├── Per-Cap:    capability_id, name, category (signal/lifecycle/route/
  │               breakthrough/tech/acquisition/loop/memory/autonomous/system),
  │               status (not_started/structural/partial/operational/mature),
  │               evidence, last_verified, verification_method
  ├── Methods:    register_capability(), verify_capability(),
  │               get_all_by_status(), get_capability_matrix(), snapshot()
  └── Data I/O:   Writes → data/evolution/capability_snapshots/

  [NEW-FILE] src/claire/evolution/verified_state_store.py
  ├── Role:       Immutable store of verified system states
  ├── Authority:  APPEND-ONLY (no modification or deletion)
  ├── Class:      VerifiedStateStore
  ├── Per-State:  state_id, timestamp, version, capabilities_verified,
  │               test_results, validation_scores, evidence_hashes, verified_by
  ├── Methods:    record_verified_state(), get_latest_verified(),
  │               get_state_at_version(v), compare_states(a, b),
  │               verify_state_integrity()
  └── Data I/O:   Writes → data/evolution/verified_states/

  [NEW-FILE] src/claire/evolution/regression_guard.py
  ├── Role:       Prevents capability regression — nothing verified can be lost
  ├── Authority:  GOVERNED
  ├── Class:      RegressionGuard
  ├── Methods:    pre_change_snapshot(), post_change_verify(),
  │               detect_regressions(), generate_regression_report(),
  │               block_if_regression()
  ├── Behavior:   HARD BLOCK on any change that breaks verified capabilities
  └── Data I/O:   Writes → data/evolution/regression_reports/

  [NEW-FILE] src/claire/evolution/forward_motion_policy.py
  ├── Role:       Governs what constitutes valid forward motion
  ├── Authority:  OWNER-ONLY
  ├── Class:      ForwardMotionPolicy
  ├── Methods:    evaluate_change(proposed_change), is_aligned_with_target(),
  │               detect_drift(), get_forward_velocity()
  └── Data I/O:   Writes → data/evolution/forward_decisions/

  [NEW-FILE] src/claire/evolution/evolution_memory.py
  ├── Role:       Long-term memory of how Claire has evolved
  ├── Authority:  SYSTEM (governed writes)
  ├── Class:      EvolutionMemory
  ├── Tracks:     decisions_made, approaches_tried, architectural_lessons,
  │               capability_build_patterns, time_to_completion_by_type
  ├── Methods:    record_evolution_event(), query_memory(topic),
  │               get_lessons_for(capability_type),
  │               get_build_velocity_trend(), get_failure_patterns()
  └── Data I/O:   Reads/Writes → data/evolution/completion_memory/

  [NEW-FILE] src/claire/evolution/evolution_orchestrator.py
  ├── Role:       Master orchestrator — coordinates all evolution subsystems
  ├── Authority:  GOVERNED
  ├── Class:      EvolutionOrchestrator
  ├── Coordinates: project_identity, master_document_registry, version_timeline,
  │               target_state, capability_registry, verified_state_store,
  │               regression_guard, forward_motion_policy, evolution_memory
  ├── Methods:    get_system_status(), propose_change(), execute_change(),
  │               get_evolution_dashboard_data()
  └── Entry Point: All evolution operations route through this orchestrator


================================================================================
PHASE 2 — UPDATE GOVERNANCE LAYER (19 files, 1 folder)
================================================================================

Purpose: No update enters the system without inspection, staging, validation,
         and rollback readiness. Self-protecting — the governance layer governs
         its own updates.

Depends On: Phase 1 (Evolution Layer — regression_guard, capability_registry,
            target_state, forward_motion_policy)

[EXISTING]  src/claire/platform/
[NEW-DIR]   src/claire/platform/update_governance/

  [NEW-FILE] src/claire/platform/update_governance/__init__.py
  ├── Role:       Update governance package init
  ├── Authority:  GOVERNED
  └── Exports:    All 18 governance modules

  [NEW-FILE] src/claire/platform/update_governance/authority_map.py
  ├── Role:       Defines WHO can authorize updates and WHAT they can touch
  ├── Authority:  OWNER-ONLY
  ├── Class:      AuthorityMap
  ├── Levels:     owner (Craig), system (Claire self-update), external
  ├── Per-Auth:   allowed_paths, denied_paths, requires_approval,
  │               max_change_scope
  ├── Methods:    check_authority(source, target_path),
  │               get_authority_for_path(path)
  ├── Protected:  lifecycle/, governance/, safety/, evolution/ → owner only
  └── Data I/O:   Reads → data/update_governance/approvals/

  [NEW-FILE] src/claire/platform/update_governance/package_inspector.py
  ├── Role:       Inspects incoming update packages BEFORE installation
  ├── Authority:  GOVERNED
  ├── Class:      PackageInspector
  ├── Checks:     file_count, file_types, size_analysis, path_analysis,
  │               conflict_detection, suspicious_pattern_detection
  ├── Methods:    inspect_package(package_path), quarantine_if_suspicious()
  └── Data I/O:   Writes → data/update_governance/inspected/

  [NEW-FILE] src/claire/platform/update_governance/source_trust.py
  ├── Role:       Trust scoring for update sources
  ├── Authority:  GOVERNED
  ├── Class:      SourceTrust
  ├── Levels:     verified, trusted, unknown, untrusted
  ├── Methods:    evaluate_source(source_id), register_source(),
  │               update_trust_after_outcome(), get_trusted_sources()
  └── Data I/O:   Reads/Writes → data/update_governance/signatures/

  [NEW-FILE] src/claire/platform/update_governance/signature_verifier.py
  ├── Role:       Cryptographic signature verification for update packages
  ├── Authority:  GOVERNED
  ├── Class:      SignatureVerifier
  ├── Verifies:   SHA-256 checksums, package-level signature, source chain
  ├── Methods:    verify_package_signature(), generate_signature(),
  │               verify_file_integrity(), sign_package()
  └── Data I/O:   Reads/Writes → data/update_governance/signatures/

  [NEW-FILE] src/claire/platform/update_governance/dependency_impact_analyzer.py
  ├── Role:       Analyzes what an update will AFFECT across the system
  ├── Authority:  GOVERNED
  ├── Class:      DependencyImpactAnalyzer
  ├── Traces:     Import graph → modules → stages → routes → tests
  ├── Methods:    analyze_impact(changed_files), get_dependency_graph(module)
  ├── Returns:    ImpactReport (affected_modules, affected_stages,
  │               affected_routes, affected_tests, risk_score)
  └── Data I/O:   Writes → data/update_governance/reports/

  [NEW-FILE] src/claire/platform/update_governance/capability_diff.py
  ├── Role:       Diffs capabilities before and after an update
  ├── Authority:  GOVERNED
  ├── Class:      CapabilityDiff
  ├── Methods:    diff_capabilities(before, after),
  │               detect_unintended_changes(), verify_no_capability_loss()
  ├── Returns:    CapabilityDiff (added, removed, modified, unchanged, degraded)
  └── Integrates: evolution/capability_registry

  [NEW-FILE] src/claire/platform/update_governance/lifecycle_stage_impact.py
  ├── Role:       Assesses which lifecycle stages (1-30) an update affects
  ├── Authority:  GOVERNED
  ├── Class:      LifecycleStageImpact
  ├── Methods:    get_affected_stages(changed_files),
  │               requires_stage_revalidation(stage),
  │               get_stage_dependency_chain(stage)
  └── Returns:    List of (stage_number, stage_name, impact_severity)

  [NEW-FILE] src/claire/platform/update_governance/runtime_risk_classifier.py
  ├── Role:       Classifies the runtime risk level of an update
  ├── Authority:  GOVERNED
  ├── Class:      RuntimeRiskClassifier
  ├── Risk Levels: safe, low, medium, high, critical
  ├── Methods:    classify_risk(update_package), get_risk_factors()
  ├── Factors:    touches_lock_zone, modifies_contracts, changes_routing,
  │               affects_governance
  └── Returns:    RiskClassification (level, factors, mitigation_required)

  [NEW-FILE] src/claire/platform/update_governance/target_alignment.py
  ├── Role:       Checks if an update moves toward or away from target state
  ├── Authority:  GOVERNED
  ├── Class:      TargetAlignment
  ├── Methods:    check_alignment(update), get_target_contribution(update),
  │               detect_target_drift(update)
  └── Integrates: evolution/target_state, evolution/forward_motion_policy

  [NEW-FILE] src/claire/platform/update_governance/evolution_gate.py
  ├── Role:       THE MASTER GATE — single go/no-go decision
  ├── Authority:  OWNER-ONLY (gate logic cannot be weakened)
  ├── Class:      EvolutionGate
  ├── Aggregates: authority_check, inspection_result, source_trust,
  │               signature_valid, dependency_impact, capability_diff,
  │               stage_impact, runtime_risk, target_alignment,
  │               regression_guard
  ├── Methods:    evaluate(update) → GateDecision
  ├── Rule:       ALL checks must pass — any single failure blocks
  └── Data I/O:   Writes → data/update_governance/approvals/

  [NEW-FILE] src/claire/platform/update_governance/migration_planner.py
  ├── Role:       Plans the migration path for approved updates
  ├── Authority:  GOVERNED
  ├── Class:      MigrationPlanner
  ├── Creates:    Ordered steps (backup, file ops, config, tests, validation)
  ├── Methods:    create_plan(approved_update), validate_plan(),
  │               get_critical_path()
  └── Data I/O:   Writes → data/update_governance/staged/

  [NEW-FILE] src/claire/platform/update_governance/protected_path_guard.py
  ├── Role:       Hard protection for paths that must NEVER be modified ungoverned
  ├── Authority:  OWNER-ONLY
  ├── Class:      ProtectedPathGuard
  ├── Protected:  src/claire/lifecycle/ (governance spine)
  │               src/claire/governance/ (gates)
  │               src/claire/safety/ (safety layer)
  │               src/claire/evolution/ (evolution layer)
  │               src/claire/platform/update_governance/ (self-protection)
  │               data/evolution/verified_states/ (immutable proofs)
  ├── Methods:    is_protected(path), check_modification_allowed(path, auth),
  │               get_protection_reason(path)
  └── Behavior:   HARD BLOCK — no override without owner authority

  [NEW-FILE] src/claire/platform/update_governance/staged_installer.py
  ├── Role:       Staged installation — stage → validate → promote
  ├── Authority:  GOVERNED
  ├── Class:      StagedInstaller
  ├── Stages:     1) Copy to staged/, 2) Run gauntlet, 3) Promote if passed
  ├── Methods:    stage_update(), validate_staged(), promote_staged(),
  │               abort_staged()
  ├── Behavior:   NEVER writes directly to live system
  └── Data I/O:   Writes → data/update_governance/staged/

  [NEW-FILE] src/claire/platform/update_governance/validation_gauntlet.py
  ├── Role:       Post-install validation — every test and check after staging
  ├── Authority:  GOVERNED
  ├── Class:      ValidationGauntlet
  ├── Checks:     1) All existing tests pass
  │               2) All stage contracts valid
  │               3) All governance gates operational
  │               4) No capability regression
  │               5) Runtime smoke test
  │               6) Import verification
  ├── Methods:    run_gauntlet(staged_update) → GauntletResult
  └── Rule:       ALL checks must pass for promotion

  [NEW-FILE] src/claire/platform/update_governance/rollback_orchestrator.py
  ├── Role:       Automatic rollback if anything goes wrong
  ├── Authority:  GOVERNED
  ├── Class:      RollbackOrchestrator
  ├── Methods:    create_rollback_point(), rollback_to(point_id),
  │               verify_rollback_integrity(), auto_rollback_on_failure()
  ├── Behavior:   Atomic — fully restores or fails safely
  └── Data I/O:   Writes → data/update_governance/rollback_points/

  [NEW-FILE] src/claire/platform/update_governance/audit_writer.py
  ├── Role:       Immutable audit trail for every update operation
  ├── Authority:  APPEND-ONLY (no modification or deletion)
  ├── Class:      AuditWriter
  ├── Logs:       inspection, gate_decision, staging, validation, promotion,
  │               rollback, approval, rejection
  ├── Per-Entry:  timestamp, action, actor, target_paths, decision,
  │               reasoning, evidence_hash
  ├── Methods:    log_action(), get_audit_trail(time_range),
  │               get_trail_for_update(update_id), verify_trail_integrity()
  └── Data I/O:   Writes → data/update_governance/reports/

  [NEW-FILE] src/claire/platform/update_governance/approval_policy.py
  ├── Role:       Defines approval requirements based on update risk level
  ├── Authority:  OWNER-ONLY
  ├── Class:      ApprovalPolicy
  ├── Policy:     safe → auto-approve
  │               low → auto-approve + logging
  │               medium → require review before promotion
  │               high → require explicit owner approval
  │               critical → owner approval + backup + rollback confirmed
  ├── Methods:    get_required_approval(risk_level),
  │               check_approval_granted(update_id), request_approval()
  └── Data I/O:   Reads/Writes → data/update_governance/approvals/

  [NEW-FILE] src/claire/platform/update_governance/update_orchestrator.py
  ├── Role:       Master orchestrator — drives entire update governance pipeline
  ├── Authority:  GOVERNED
  ├── Class:      UpdateOrchestrator
  ├── Pipeline:   1)  Receive update
  │               2)  package_inspector.inspect()
  │               3)  source_trust.evaluate()
  │               4)  signature_verifier.verify()
  │               5)  authority_map.check()
  │               6)  dependency_impact_analyzer.analyze()
  │               7)  capability_diff.diff()
  │               8)  lifecycle_stage_impact.assess()
  │               9)  runtime_risk_classifier.classify()
  │               10) target_alignment.check()
  │               11) evolution_gate.evaluate() — GO / NO-GO
  │               12) approval_policy.get_required()
  │               13) migration_planner.create_plan()
  │               14) rollback_orchestrator.create_rollback_point()
  │               15) staged_installer.stage()
  │               16) validation_gauntlet.run()
  │               17) staged_installer.promote() OR rollback()
  │               18) audit_writer.log()
  └── Entry Point: process_update(package_path)


================================================================================
PHASE 3 — API ROUTE & FRONTEND PANELS (4 files, 1 folder)
================================================================================

Purpose: Expose evolution & governance data via API, provide 3 frontend panels
         for real-time system visibility.

Depends On: Phase 1 + Phase 2

[EXISTING]  src/claire/api/
[NEW-FILE]  src/claire/api/routes_update_governance.py
  ├── Role:       API routes for update governance + evolution systems
  ├── Authority:  GOVERNED
  ├── Endpoints:
  │   ├── GET  /api/v3/governance/update/status          Pipeline status
  │   ├── POST /api/v3/governance/update/inspect          Trigger inspection
  │   ├── POST /api/v3/governance/update/approve          Owner approval
  │   ├── GET  /api/v3/governance/update/audit            Audit trail
  │   ├── GET  /api/v3/governance/update/rollback-points  Available rollbacks
  │   ├── POST /api/v3/governance/update/rollback         Trigger rollback
  │   ├── GET  /api/v3/evolution/status                   Full evolution status
  │   ├── GET  /api/v3/evolution/identity                 Project identity
  │   ├── GET  /api/v3/evolution/timeline                 Version timeline
  │   ├── GET  /api/v3/evolution/capabilities             Capability matrix
  │   ├── GET  /api/v3/evolution/target                   Target + gap analysis
  │   └── GET  /api/v3/evolution/regressions              Regression reports
  └── Integrates: update_orchestrator, evolution_orchestrator

[EXISTING]  src/frontend/
[NEW-DIR]   src/frontend/system/

  [NEW-FILE] src/frontend/system/UpdateGovernancePanel.js
  ├── Role:       Frontend — update governance pipeline visualization
  ├── Displays:   Update queue, inspection results, gate decisions,
  │               staging status, gauntlet results, approval requests,
  │               rollback points, audit trail
  ├── Interactive: Approve/reject, trigger rollback, inspect details
  └── Color Code: Green (passed), Red (blocked), Yellow (pending), Gray (idle)

  [NEW-FILE] src/frontend/system/EvolutionMemoryPanel.js
  ├── Role:       Frontend — evolution memory and project identity
  ├── Displays:   Project identity, version timeline (v1→current),
  │               target state + completion %, capability matrix,
  │               evolution velocity chart, forward motion trend,
  │               lessons learned feed
  └── Interactive: Drill into versions, filter capabilities, compare snapshots

  [NEW-FILE] src/frontend/system/RegressionGuardPanel.js
  ├── Role:       Frontend — regression detection and prevention
  ├── Displays:   Verified state history, regression scan results,
  │               regression reports, protected paths, capability diffs
  ├── Interactive: Trigger scan, view details, compare verified states
  └── Alerts:     Red/green per capability, alert on detected regression


================================================================================
PHASE 4 — DATA DIRECTORIES (0 files, 18 folders)
================================================================================

Purpose: Governed data storage for evolution state and update governance
         artifacts. All directories get .gitkeep markers.

Depends On: Nothing (can be created independently)

[NEW-DIR]   data/evolution/
[NEW-DIR]   ├── data/evolution/origin/               ← Governing document records
[NEW-DIR]   ├── data/evolution/identity/              ← Project identity data
[NEW-DIR]   ├── data/evolution/timeline/              ← Version history records
[NEW-DIR]   ├── data/evolution/target_state/          ← Target completion state
[NEW-DIR]   ├── data/evolution/verified_states/       ← Immutable verified states (APPEND-ONLY)
[NEW-DIR]   ├── data/evolution/capability_snapshots/  ← Capability snapshots over time
[NEW-DIR]   ├── data/evolution/regression_reports/    ← Regression scan results
[NEW-DIR]   ├── data/evolution/forward_decisions/     ← Forward motion decisions
[NEW-DIR]   └── data/evolution/completion_memory/     ← Evolution learning memory

[NEW-DIR]   data/update_governance/
[NEW-DIR]   ├── data/update_governance/incoming/       ← Inbound update packages
[NEW-DIR]   ├── data/update_governance/inspected/      ← Post-inspection reports
[NEW-DIR]   ├── data/update_governance/staged/         ← Staged (pre-promotion) files
[NEW-DIR]   ├── data/update_governance/backups/        ← Pre-update backups
[NEW-DIR]   ├── data/update_governance/reports/        ← Impact, audit, gauntlet reports
[NEW-DIR]   ├── data/update_governance/approvals/      ← Approval decisions + gate logs
[NEW-DIR]   ├── data/update_governance/rollback_points/← Rollback snapshots
[NEW-DIR]   ├── data/update_governance/signatures/     ← Signatures, keys, trust records
[NEW-DIR]   └── data/update_governance/quarantined/    ← Suspicious packages held for review


================================================================================
INTEGRATION MAP — HOW THE TWO SUBSYSTEMS CONNECT
================================================================================

  ┌──────────────────────────────────────────────────────────────────┐
  │                     UPDATE ARRIVES                               │
  └──────────────┬───────────────────────────────────────────────────┘
                 │
                 ▼
  ┌──────────────────────────────┐
  │  package_inspector           │  Inspect structure, detect threats
  │  source_trust                │  Score source credibility
  │  signature_verifier          │  Verify cryptographic integrity
  │  authority_map               │  Check authorization for target paths
  └──────────────┬───────────────┘
                 │
                 ▼
  ┌──────────────────────────────┐
  │  dependency_impact_analyzer  │  Trace system-wide impact
  │  capability_diff             │◄── evolution/capability_registry
  │  lifecycle_stage_impact      │  Map to 30-stage lifecycle
  │  runtime_risk_classifier     │  Classify risk level
  │  target_alignment            │◄── evolution/target_state
  │                              │◄── evolution/forward_motion_policy
  └──────────────┬───────────────┘
                 │
                 ▼
  ┌──────────────────────────────┐
  │  evolution_gate              │  MASTER GO / NO-GO
  │  ├── ALL checks must pass    │◄── evolution/regression_guard
  │  └── Single failure = BLOCK  │
  └──────────────┬───────────────┘
                 │ (APPROVED)
                 ▼
  ┌──────────────────────────────┐
  │  approval_policy             │  Risk-based approval routing
  │  migration_planner           │  Ordered migration steps
  │  rollback_orchestrator       │  Create rollback point
  └──────────────┬───────────────┘
                 │
                 ▼
  ┌──────────────────────────────┐
  │  staged_installer            │  Install to staging area
  │  validation_gauntlet         │  Run ALL tests + checks
  │  ├── PASS → promote to live  │
  │  └── FAIL → auto rollback    │
  └──────────────┬───────────────┘
                 │
                 ▼
  ┌──────────────────────────────┐
  │  audit_writer                │  Immutable audit record
  │  evolution_memory            │◄── evolution/evolution_memory
  │  (Record outcome for future) │
  └──────────────────────────────┘


================================================================================
PROTECTED PATHS — HARD-BLOCKED WITHOUT OWNER AUTHORITY
================================================================================

  Path                                    Reason
  ─────────────────────────────────────── ─────────────────────────────────────
  src/claire/lifecycle/                   30-stage governance spine
  src/claire/governance/                  Gate logic and governance rules
  src/claire/safety/                      Safety layer — never weakened
  src/claire/evolution/                   Evolution layer (self-awareness)
  src/claire/platform/update_governance/  Self-protection (this layer)
  data/evolution/verified_states/         Immutable proof records


================================================================================
INSTALL SEQUENCE
================================================================================

  Recommended order:
    Phase 4 (data dirs)  → No dependencies, creates storage first
    Phase 1 (evolution)  → Foundational self-awareness layer
    Phase 2 (governance) → Update pipeline, depends on Phase 1
    Phase 3 (API + UI)   → Exposure layer, depends on Phase 1 + 2

  Installer handles ordering automatically when run without --phase flag.

  Commands:
    Dry run (default):    python claire_v10.3.1_evolution_governance_installer.py
    Full install:         python claire_v10.3.1_evolution_governance_installer.py --install
    Single phase:         python claire_v10.3.1_evolution_governance_installer.py --install --phase 1
    Custom target:        python claire_v10.3.1_evolution_governance_installer.py --install --target ./Claire


================================================================================
END OF FILE MAP — v10.3.1 Evolution & Update Governance
================================================================================
